# Sitni dug (čišćenje) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Počistiti nagomilani sitni dug: mrtav kod u `ops/setup.py`, llmfit keš po procesu, rehash legacy 200k lozinki pri prijavi, conftest helper za `mark_complete`, dupli lokalni `run_isolated` importi, stale ime testa + dva jednoretka iz deferred minora (out=out, ROLES).

**Architecture:** Dvije nezavisne cjeline: (1) mehaničko brisanje/preimenovanje bez promjene ponašanja, (2) dva mala ponašanja (keš + rehash) s testovima. Bez novih modula.

**Tech Stack:** Python 3.11+ stdlib; postojeći pytest obrasci.

## Global Constraints

- Hrvatski latinica **s dijakriticima** u stringovima/komentarima (cyrillic-gate); bez ćirilice.
- Bez novih dependencyja.
- Testovi bez mreže, bez stdina, bez pravih subprocessa.
- Commit poruke: hrvatske konvencionalne, footer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Ne mijenjaj javna ponašanja osim dva eksplicitno navedena (llmfit keš, rehash-on-login).

---

### Task 1: Mehaničko čišćenje (mrtav kod, importi, imena, helper)

**Files:**
- Modify: `ragspine/ops/setup.py`, `ragspine/ops/preflight.py` (retci ~77 i ~387), `ragspine/docs/ocr.py` (redak ~131), `ragspine/ops/wizard.py:291`, `ragspine/business/folders.py:11`
- Modify: `tests/test_setup.py`, `tests/test_wizard.py`, `tests/conftest.py` + testne datoteke s `mark_complete` call siteovima
- Test: postojeći suite (bez novih testova — čišćenje bez promjene ponašanja)

**Interfaces:**
- Consumes: postojeće.
- Produces: `tests/conftest.py::complete_setup(spine)` — jedini novi simbol. `ragspine/ops/setup.py` nakon taska izvozi SAMO `_ram_gb` i `detect_hw` (potrošači: `preflight.system_state`, `model_recommender.recommend` — potpisi nepromijenjeni).

- [ ] **Step 1: Obriši mrtav kod u `ragspine/ops/setup.py`**

Provjereno prije plana: `setup.run`, `setup.llmfit`, `setup.detect_providers`, `setup._year`, `setup.ENV_KEY_NAMES` nemaju produkcijskog pozivatelja (jedini korisnici su međusobni pozivi + `tests/test_setup.py`). Obriši tih 5 simbola i SVE importe koji nakon toga ostanu neiskorišteni (`json`, `urllib.request`, `date`, `optional`, `_ollama_alive`, `load_oauth_token`, `get_spine`, `seeds` — provjeri svaki grep-om po datoteci prije brisanja). Ostaju: `_ram_gb`, `detect_hw` + njihovi importi (`os`, `platform`, `shutil`, `Path`, `run_isolated`). Ažuriraj module docstring u: `"""Detekcija hardvera (za preflight/model_recommender). Stari setup-report je zamijenio wizard (ragspine setup)."""`

- [ ] **Step 2: Uskladi `tests/test_setup.py`**

Obriši `test_llmfit_absent_returns_none`, `test_detect_providers_shape_and_no_secret_leak`, `test_run_returns_report_string` + importe koji ostanu neiskorišteni (npr. `init_spine` ako ga koristi samo obrisani test). Seeds i `test_detect_hw_shape` testovi ostaju.

- [ ] **Step 3: `run_isolated` importi**

- `ragspine/ops/preflight.py`: top-level import postoji (redak 18) — obriši lokalne `from ragspine.core.subproc import run_isolated` unutar funkcija (retci ~77 i ~387).
- `ragspine/docs/ocr.py`: premjesti lokalni import (redak ~131) u top-level import blok (abecedno). `core.subproc` je lagan (stdlib) — nema cikličkog rizika.

- [ ] **Step 4: Stale ime testa**

`tests/test_wizard.py`: preimenuj `test_run_resume_from_stage2_runs_only_model_page` → `test_run_resume_from_stage2_runs_pages_3_to_6`; docstring skrati na: `"""Resume od stage 2 pokreće stranice 3-6 (model, mreža, mape, sažetak) — ne ponavlja 1-2."""`

- [ ] **Step 5: Deferred jednoretci**

- `ragspine/ops/wizard.py:291`: `certs.generate_self_signed(cert_dir, ips=[cert_ip], hostnames=["ragspine.local"])` → dodaj `out=out` (funkcija ima keyword-only `out=print`; sve ostalo u `page_mreza` već ide kroz `out`).
- `ragspine/business/folders.py:11`: `ROLES = ("propisi", "klijenti", "ostalo", "skener")` → dodaj `"program"` na kraj tuple-a (wizard stranica 5 ga koristi; ROLES je dokumentacijski prijedlog, nigdje se ne enforca).

- [ ] **Step 6: conftest `complete_setup` helper + call siteovi**

U `tests/conftest.py` (iza fixturea) dodaj:

```python
def complete_setup(spine):
    """Gatekeeper drži web na /ui/setup dok setup wizard ne završi — testovi
    koji gađaju API/UI rute ovim označe setup dovršenim (jedno mjesto za
    incantation umjesto raspršenih wizard_state.mark_complete poziva)."""
    from ragspine.ops import wizard_state
    wizard_state.mark_complete(spine)
```

Zatim u testnim datotekama koje zovu `ws.mark_complete(...)`/`wizard_state.mark_complete(...)` SAMO kao gatekeeper-incantation (uz komentar „gatekeeper drži...") zamijeni poziv s `complete_setup(...)` + import `from tests.conftest import complete_setup` (tests je paket — `tests/__init__.py` postoji). Ako datoteka `ws`/`wizard_state` koristi i za drugo (`set_stage`, `get_stage`, `reset`, asserte nad stanjem), zadrži taj import; ako ostane neiskorišten, obriši ga. **Ne diraj** `tests/test_wizard_state.py`, `tests/test_wizard.py` i `tests/test_firstrun.py` — oni testiraju sam mehanizam. Pronađi call siteove s: `grep -rn "mark_complete(" tests/ --include="*.py"`.

- [ ] **Step 7: Puni suite**

Run: `python -m pytest -q`
Expected: isti broj passed kao baseline minus 3 obrisana testa (baseline 1129 passed, 1 skipped → očekivano 1126 passed, 1 skipped).

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "chore(dug): obriši mrtvi ops/setup report, dupli run_isolated importi, conftest complete_setup helper, stale imena"
```

---

### Task 2: llmfit keš po procesu + rehash-on-login (legacy 200k)

**Files:**
- Modify: `ragspine/ops/preflight.py` (llmfit_models, redak ~234), `ragspine/web/api.py` (login handler, iza retka ~521), `tests/conftest.py` (autouse reset)
- Test: `tests/test_preflight.py`, `tests/test_api_auth.py`

**Interfaces:**
- Consumes: `preflight.llmfit_models(cfg=None)` (potpis nepromijenjen), login handler `POST /auth/login` u `create_app` (koristi `verify_password`, `hash_password` — oba već importirana u api.py:44), `spine.write()`.
- Produces: `preflight._llmfit_cache` modul-global (testovi ga resetiraju); rehash side-effect na `users.pw_hash` pri uspješnoj prijavi s legacy hashom.

- [ ] **Step 1: Napiši failing testove**

U `tests/test_preflight.py`:

```python
def test_llmfit_models_kesira_po_procesu(monkeypatch):
    calls = []
    payload = '{"models": []}'

    def _fake_run(cmd, timeout=60, mem_mb=1024):
        calls.append(cmd)
        return 0, payload, ""
    monkeypatch.setattr(preflight, "run_isolated", _fake_run)
    monkeypatch.setattr(preflight, "system_state", lambda cfg=None: {"ram_total_gb": 16})
    first = preflight.llmfit_models(None)
    second = preflight.llmfit_models(None)
    assert first == [] and second == []
    assert len(calls) == 1          # drugi poziv ide iz keša


def test_llmfit_models_ne_kesira_neuspjeh(monkeypatch):
    rcs = iter([1, 0])
    monkeypatch.setattr(preflight, "run_isolated",
                        lambda cmd, timeout=60, mem_mb=1024: (next(rcs), '{"models": []}', ""))
    monkeypatch.setattr(preflight, "system_state", lambda cfg=None: {"ram_total_gb": 16})
    assert preflight.llmfit_models(None) is None    # rc=1 → None, ne kešira se
    assert preflight.llmfit_models(None) == []      # sljedeći pokušaj ponovno pokreće
```

U `tests/test_api_auth.py` (na kraju):

```python
def test_login_rehasha_legacy_200k_hash(spine, cfg):
    import hashlib
    salt = b"\x01" * 16
    h = hashlib.pbkdf2_hmac("sha256", b"tajna", salt, 200_000)
    legacy = f"{salt.hex()}${h.hex()}"
    with spine.write() as c:
        c.execute("INSERT INTO users(username, pw_hash, role) VALUES(?,?,?)",
                  ("stari", legacy, "owner"))
    c2 = _client(spine, cfg)
    r = c2.post("/auth/login", json={"username": "stari", "password": "tajna"})
    assert r.status_code == 200
    row = spine.read().execute(
        "SELECT pw_hash FROM users WHERE username=?", ("stari",)).fetchone()
    assert row["pw_hash"].startswith("pbkdf2$600000$")
    # prijava radi i s novim hashom
    assert c2.post("/auth/login",
                   json={"username": "stari", "password": "tajna"}).status_code == 200
```

Napomena: ako `users` tablica ima dodatne NOT NULL kolone bez defaulta, prilagodi INSERT (pogledaj kako `add_user`/postojeći testovi ubacuju korisnike) — ali hash mora ostati legacy format.

- [ ] **Step 2: Pokreni testove — moraju pasti**

Run: `python -m pytest tests/test_preflight.py -k kesira -v && python -m pytest tests/test_api_auth.py -k rehash -v`
Expected: FAIL — keš ne postoji (2 poziva run_isolated); pw_hash ostaje legacy.

- [ ] **Step 3: Implementiraj llmfit keš**

U `ragspine/ops/preflight.py`, iznad `llmfit_models`:

```python
# llmfit subprocess je spor (~sekunde); wizard i web preflight ga zovu više
# puta u procesu. Keš po procesu; None (neuspjeh) se NE kešira da
# retry-nakon-installa proradi. Testovi resetiraju kroz conftest.
_llmfit_cache: list[dict] | None = None
```

U `llmfit_models` na početak tijela:

```python
    global _llmfit_cache
    if _llmfit_cache is not None:
        return _llmfit_cache
```

a završni `return deduped[:_LLMFIT_MAX_ROWS]` zamijeni s:

```python
    _llmfit_cache = deduped[:_LLMFIT_MAX_ROWS]
    return _llmfit_cache
```

U `tests/conftest.py` dodaj autouse fixture (uz postojeći `_reset_embed_globals` obrazac):

```python
@pytest.fixture(autouse=True)
def _reset_llmfit_cache():
    """llmfit keš po procesu ne smije procuriti između testova."""
    from ragspine.ops import preflight
    preflight._llmfit_cache = None
    yield
    preflight._llmfit_cache = None
```

- [ ] **Step 4: Implementiraj rehash-on-login**

U `ragspine/web/api.py`, u login handleru, ODMAH iza uspješne provjere (`if not verify_password(password, row["pw_hash"]): raise ...`), dodaj:

```python
        if not row["pw_hash"].startswith("pbkdf2$"):
            # legacy 200k hash (bez prefiksa): rehash na aktualne parametre
            # pri uspješnoj prijavi — lozinku imamo samo sada, u plaintextu.
            with spine.write() as c:
                c.execute("UPDATE users SET pw_hash=? WHERE id=?",
                          (hash_password(password), row["id"]))
```

- [ ] **Step 5: Pokreni ciljane pa puni suite**

Run: `python -m pytest tests/test_preflight.py tests/test_api_auth.py -v` → PASS
Run: `python -m pytest -q` → očekivano 1129 passed, 1 skipped (1126 iz Taska 1 + 3 nova).

- [ ] **Step 6: Commit**

```bash
git add ragspine/ops/preflight.py ragspine/web/api.py tests/conftest.py tests/test_preflight.py tests/test_api_auth.py
git commit -m "feat(dug): llmfit keš po procesu; rehash legacy 200k lozinke pri prijavi"
```

---

## Self-Review (obavljen pri pisanju plana)

1. **Pokrivenost stavki:** mrtav kod ✓ T1/S1-2; llmfit keš ✓ T2; rehash 200k ✓ T2; conftest helper ✓ T1/S6; run_isolated importi ✓ T1/S3; stale ime ✓ T1/S4; + 2 deferred jednoretka ✓ T1/S5.
2. **Placeholderi:** nema — kod i brojevi konkretni; jedina delegirana provjera (INSERT kolone) ima jasnu uputu gdje pogledati.
3. **Konzistentnost:** `hash_password` iters = 600k (`_PBKDF2_ITERS`) → assert `pbkdf2$600000$` odgovara formatu `f"pbkdf2${_PBKDF2_ITERS}$..."`; `run_isolated` potpis `(cmd, timeout=, mem_mb=)` odgovara pozivu u `llmfit_models`.
