# RAGSPINE v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** AI asistent za hrvatske knjigovođe — chat s RAG-om nad vlastitim dokumentima, watchlista zakona, kalkulatori, poslovni alati, OCR, browser bridge. Sve u jednom Python paketu, `python -m ragspine serve`.

**Architecture:** FastAPI + SQLite WAL (raw SQL, per-thread read konekcije + write lock). Hibridni retrieval FTS5 + sqlite-vec (optional) + RRF. Vlastiti urllib LLM dispatcher (OpenAI-compat + Anthropic + OAuth fallback). Sve teške ovisnosti optional s degradacijom kroz `core/optional.py`. `config_overrides` tablica = centralni mehanizam za dinamičke vrijednosti.

**Tech Stack:** Python 3.11+, FastAPI, uvicorn, pydantic. Optional: PyMuPDF, fastembed, sqlite-vec, python-docx, openpyxl, browser-use, apprise.

## Global Constraints

- Python 3.11+ (dev box ima 3.13).
- Core MORA raditi samo s `fastapi` + `uvicorn` + stdlib. Svaki drugi import ide kroz `core/optional.py:need()`.
- Raw SQL, bez ORM-a. Jedan DB fajl, WAL mod.
- Stdlib gdje može: urllib (ne requests), hashlib+hmac za JWT.
- Sav novi kod TDD: test prvo, fail, implementacija, pass, commit.
- SSRF guard na SVIM vanjskim fetchevima (`core/net.py:safe_fetch`).
- Radni direktorij: `/home/matej/Desktop/RAGSPINE/CLAUDE/v1/`. Testovi: `python -m pytest tests/ -x -q`.
- Commit poruke: `feat|fix|test|docs: ...` + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Domenske brojke (stope, dnevnice, minimalac) su DEFAULTI s `source` poljem, uvijek overridable kroz config_overrides. Točnost se održava kroz learn/watchlist, ne hardcode.
- Porezni model 2026: prirez UKINUT (2024). Model: državne stope 20%/30%, grad može imati vlastite kroz override `porez_niza.<Grad>`/`porez_visa.<Grad>`; legacy `prirez.<Grad>` override se poštuje kao dodatak (spec compat).

---

## Faza A — temelj

### Task 1: Skeleton + config.py + core/optional.py

**Files:**
- Create: `pyproject.toml`, `ragspine/__init__.py`, `ragspine/config.py`, `ragspine/core/__init__.py`, `ragspine/core/optional.py`, `tests/__init__.py`, `tests/conftest.py`
- Test: `tests/test_config.py`, `tests/test_optional.py`

**Interfaces:**
- Produces: `ragspine.config.Config` (dataclass, `Config.from_env()`), globalni `get_config()/set_config(cfg)`; `ragspine.core.optional.need(module: str, feature: str) -> module|None`, `missing() -> dict[str, str]` (feature → install hint).
- Config polja (env `RAGSPINE_*`): `data_dir` (default `~/.ragspine`), `db_path` (default `{data_dir}/ragspine.db`), `host` (127.0.0.1), `port` (8400), `llm_base_url`, `llm_api_key`, `llm_model`, `anthropic_base_url` (`https://api.anthropic.com`), `ollama_url` (`http://127.0.0.1:11434`), `ocr_url`, `embed_model` (`BAAI/bge-m3`), `nas_root`, `imap_host`, `imap_user`, `imap_pass`, `jwt_secret` (ako nije zadan: generiraj i persistaj u `{data_dir}/secret`), `redact_pii` (bool, default False), `egress_allow` (CSV hostova kojima je dopušten privatni IP).

- [ ] **Step 1: pyproject.toml + prazni paketi**

```toml
[project]
name = "ragspine"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.110", "uvicorn>=0.29", "pydantic>=2"]

[project.optional-dependencies]
full = ["pymupdf", "fastembed", "sqlite-vec", "python-docx", "openpyxl", "apprise"]

[tool.pytest.ini_options]
testpaths = ["tests"]
```

- [ ] **Step 2: failing testovi**

```python
# tests/test_config.py
import os
from ragspine.config import Config

def test_defaults(tmp_path, monkeypatch):
    monkeypatch.setenv("RAGSPINE_DATA_DIR", str(tmp_path))
    cfg = Config.from_env()
    assert cfg.port == 8400
    assert cfg.db_path == str(tmp_path / "ragspine.db")

def test_env_override(tmp_path, monkeypatch):
    monkeypatch.setenv("RAGSPINE_DATA_DIR", str(tmp_path))
    monkeypatch.setenv("RAGSPINE_PORT", "9000")
    assert Config.from_env().port == 9000

def test_jwt_secret_persisted(tmp_path, monkeypatch):
    monkeypatch.setenv("RAGSPINE_DATA_DIR", str(tmp_path))
    s1 = Config.from_env().jwt_secret
    s2 = Config.from_env().jwt_secret
    assert s1 == s2 and len(s1) >= 32
```

```python
# tests/test_optional.py
from ragspine.core import optional

def test_need_present():
    assert optional.need("json", "test-feature") is not None

def test_need_missing_registers():
    assert optional.need("nepostojeci_modul_xyz", "vektorska pretraga") is None
    assert "vektorska pretraga" in optional.missing()
```

- [ ] **Step 3: Run — expect FAIL (ModuleNotFoundError)**
- [ ] **Step 4: Implementacija**

```python
# ragspine/core/optional.py
"""Optional import + registar nedostajućih featurea."""
import importlib
_missing: dict[str, str] = {}

def need(module: str, feature: str):
    try:
        return importlib.import_module(module)
    except ImportError:
        _missing[feature] = f"pip install {module.replace('_','-').split('.')[0]}"
        return None

def missing() -> dict[str, str]:
    return dict(_missing)
```

```python
# ragspine/config.py — dataclass, from_env čita RAGSPINE_* env,
# jwt_secret: env RAGSPINE_JWT_SECRET ili secrets.token_hex(32) persistan
# u {data_dir}/secret (chmod 0600). get_config() lazy singleton, set_config za testove.
from dataclasses import dataclass, field
import os, secrets
from pathlib import Path

@dataclass
class Config:
    data_dir: str; db_path: str; host: str; port: int
    llm_base_url: str; llm_api_key: str; llm_model: str
    anthropic_base_url: str; ollama_url: str; ocr_url: str
    embed_model: str; nas_root: str
    imap_host: str; imap_user: str; imap_pass: str
    jwt_secret: str; redact_pii: bool; egress_allow: list[str]

    @classmethod
    def from_env(cls) -> "Config":
        e = os.environ.get
        data_dir = os.path.expanduser(e("RAGSPINE_DATA_DIR", "~/.ragspine"))
        Path(data_dir).mkdir(parents=True, exist_ok=True)
        secret = e("RAGSPINE_JWT_SECRET", "")
        if not secret:
            sf = Path(data_dir) / "secret"
            if sf.exists():
                secret = sf.read_text().strip()
            else:
                secret = secrets.token_hex(32)
                sf.write_text(secret); sf.chmod(0o600)
        return cls(
            data_dir=data_dir,
            db_path=e("RAGSPINE_DB_PATH", str(Path(data_dir) / "ragspine.db")),
            host=e("RAGSPINE_HOST", "127.0.0.1"), port=int(e("RAGSPINE_PORT", "8400")),
            llm_base_url=e("RAGSPINE_LLM_BASE_URL", ""), llm_api_key=e("RAGSPINE_LLM_API_KEY", ""),
            llm_model=e("RAGSPINE_LLM_MODEL", ""),
            anthropic_base_url=e("RAGSPINE_ANTHROPIC_BASE_URL", "https://api.anthropic.com"),
            ollama_url=e("RAGSPINE_OLLAMA_URL", "http://127.0.0.1:11434"),
            ocr_url=e("RAGSPINE_OCR_URL", ""), embed_model=e("RAGSPINE_EMBED_MODEL", "BAAI/bge-m3"),
            nas_root=e("RAGSPINE_NAS_ROOT", ""), imap_host=e("RAGSPINE_IMAP_HOST", ""),
            imap_user=e("RAGSPINE_IMAP_USER", ""), imap_pass=e("RAGSPINE_IMAP_PASS", ""),
            jwt_secret=secret, redact_pii=e("RAGSPINE_REDACT_PII", "0") == "1",
            egress_allow=[h for h in e("RAGSPINE_EGRESS_ALLOW", "").split(",") if h])

_cfg: Config | None = None
def get_config() -> Config:
    global _cfg
    if _cfg is None: _cfg = Config.from_env()
    return _cfg
def set_config(cfg: Config | None): 
    global _cfg; _cfg = cfg
```

```python
# tests/conftest.py
import pytest
from ragspine.config import Config, set_config

@pytest.fixture
def cfg(tmp_path, monkeypatch):
    monkeypatch.setenv("RAGSPINE_DATA_DIR", str(tmp_path))
    c = Config.from_env(); set_config(c)
    yield c
    set_config(None)
```

- [ ] **Step 5: Run — PASS; Step 6: Commit** `feat: skeleton, config, optional-deps registry`

### Task 2: core/spine.py — baza

**Files:**
- Create: `ragspine/core/spine.py`
- Test: `tests/test_spine.py`

**Interfaces:**
- Produces: `Spine(db_path)`: `.read() -> sqlite3.Connection` (per-thread, row_factory=Row), `.write()` contextmanager (lock + commit/rollback), `.get_override(module, key, default=None) -> str|default`, `.set_override(module, key, value, source_url="")`, `.audit(user, action, entity="", detail="")`, `.close()`. Modul-level `init_spine(db_path) -> Spine`, `get_spine() -> Spine`.
- `SCHEMA`: sve core tablice (vidi listu u specu §Baza) + FTS5 `chunks_fts(content, title)` external-content nad `chunks`. sqlite-vec tablica se kreira u Tasku 8 samo ako je modul dostupan.

- [ ] **Step 1: failing testovi**

```python
# tests/test_spine.py
import threading
from ragspine.core.spine import Spine

def _sp(tmp_path): return Spine(str(tmp_path / "t.db"))

def test_schema_created(tmp_path):
    sp = _sp(tmp_path)
    names = {r[0] for r in sp.read().execute(
        "SELECT name FROM sqlite_master WHERE type='table'")}
    for t in ["documents", "chunks", "clients", "users", "watch_sources",
              "config_overrides", "audit_log", "kg_edges", "obligations",
              "query_cache", "knowledge", "kontni_plan", "hash_chain"]:
        assert t in names, t
    assert sp.read().execute("PRAGMA journal_mode").fetchone()[0] == "wal"

def test_override_roundtrip(tmp_path):
    sp = _sp(tmp_path)
    assert sp.get_override("kalkulator", "prirez.Split", "0") == "0"
    sp.set_override("kalkulator", "prirez.Split", "15.0", "https://porezna.hr")
    assert sp.get_override("kalkulator", "prirez.Split") == "15.0"

def test_concurrent_writes(tmp_path):
    sp = _sp(tmp_path)
    def w(i):
        with sp.write() as c:
            c.execute("INSERT INTO notes(client_id, author, body) VALUES (0,?,?)", ("t", str(i)))
    ts = [threading.Thread(target=w, args=(i,)) for i in range(20)]
    [t.start() for t in ts]; [t.join() for t in ts]
    assert sp.read().execute("SELECT COUNT(*) FROM notes").fetchone()[0] == 20

def test_audit(tmp_path):
    sp = _sp(tmp_path)
    sp.audit("ana", "pdv_sent", "client:3", "srpanj")
    r = sp.read().execute("SELECT user, action FROM audit_log").fetchone()
    assert (r["user"], r["action"]) == ("ana", "pdv_sent")
```

- [ ] **Step 2: Run — FAIL. Step 3: Implementacija**

```python
# ragspine/core/spine.py — ključni dijelovi
import sqlite3, threading, time
from contextlib import contextmanager

SCHEMA = """
CREATE TABLE IF NOT EXISTS documents(id INTEGER PRIMARY KEY, title TEXT, path TEXT,
  doc_type TEXT, client_id INTEGER, sha256 TEXT UNIQUE, source_url TEXT,
  valid_from TEXT, valid_until TEXT, stale INTEGER DEFAULT 0,
  created_at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS chunks(id INTEGER PRIMARY KEY, doc_id INTEGER, seq INTEGER,
  text TEXT, title TEXT);
CREATE VIRTUAL TABLE IF NOT EXISTS chunks_fts USING fts5(text, title,
  content='chunks', content_rowid='id', tokenize='unicode61 remove_diacritics 2');
CREATE TRIGGER IF NOT EXISTS chunks_ai AFTER INSERT ON chunks BEGIN
  INSERT INTO chunks_fts(rowid, text, title) VALUES (new.id, new.text, new.title); END;
CREATE TRIGGER IF NOT EXISTS chunks_ad AFTER DELETE ON chunks BEGIN
  INSERT INTO chunks_fts(chunks_fts, rowid, text, title) VALUES ('delete', old.id, old.text, old.title); END;
CREATE TABLE IF NOT EXISTS clients(id INTEGER PRIMARY KEY, name TEXT, oib TEXT UNIQUE,
  email TEXT, phone TEXT, owner TEXT, industry TEXT, pdv_status TEXT, nas_folder TEXT,
  active INTEGER DEFAULT 1);
CREATE TABLE IF NOT EXISTS users(id INTEGER PRIMARY KEY, username TEXT UNIQUE,
  pw_hash TEXT, role TEXT DEFAULT 'radnik', created_at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS config_overrides(module TEXT, key TEXT, value TEXT,
  source_url TEXT, updated_at TEXT DEFAULT (datetime('now')), PRIMARY KEY(module, key));
CREATE TABLE IF NOT EXISTS watch_sources(id INTEGER PRIMARY KEY, url TEXT UNIQUE,
  category TEXT, client_id INTEGER, added_by TEXT, active INTEGER DEFAULT 1, kind TEXT DEFAULT 'page');
CREATE TABLE IF NOT EXISTS watch_state(source_id INTEGER PRIMARY KEY, last_hash TEXT,
  last_checked TEXT, last_content TEXT);
CREATE TABLE IF NOT EXISTS law_versions(id INTEGER PRIMARY KEY, source_id INTEGER,
  content TEXT, fetched_at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS upcoming_changes(id INTEGER PRIMARY KEY, source_id INTEGER,
  description TEXT, effective_date TEXT, detected_at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS obligations(id INTEGER PRIMARY KEY, client_id INTEGER,
  kind TEXT, period TEXT, UNIQUE(client_id, kind, period));
CREATE TABLE IF NOT EXISTS obligation_status(obligation_id INTEGER PRIMARY KEY,
  sent INTEGER DEFAULT 0, sent_by TEXT, sent_at TEXT);
CREATE TABLE IF NOT EXISTS notes(id INTEGER PRIMARY KEY, client_id INTEGER, author TEXT,
  body TEXT, created_at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS audit_log(id INTEGER PRIMARY KEY, user TEXT, action TEXT,
  entity TEXT, detail TEXT, at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS hash_chain(id INTEGER PRIMARY KEY, event TEXT, prev_hash TEXT,
  hash TEXT, at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS knowledge(id INTEGER PRIMARY KEY, question TEXT, answer TEXT,
  category TEXT, tags TEXT, hits INTEGER DEFAULT 0, created_at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS reminders(id INTEGER PRIMARY KEY, user TEXT, body TEXT,
  due TEXT, done INTEGER DEFAULT 0);
CREATE TABLE IF NOT EXISTS feedback(id INTEGER PRIMARY KEY, user TEXT, query TEXT,
  answer_id TEXT, score INTEGER, comment TEXT, at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS memory(id INTEGER PRIMARY KEY, user TEXT, key TEXT, value TEXT,
  UNIQUE(user, key));
CREATE TABLE IF NOT EXISTS kontni_plan(konto TEXT PRIMARY KEY, naziv TEXT, razred TEXT);
CREATE TABLE IF NOT EXISTS cjenik(id INTEGER PRIMARY KEY, usluga TEXT, cijena REAL, valuta TEXT DEFAULT 'EUR');
CREATE TABLE IF NOT EXISTS kg_nodes(id INTEGER PRIMARY KEY, kind TEXT, value TEXT, UNIQUE(kind, value));
CREATE TABLE IF NOT EXISTS kg_edges(src INTEGER, dst INTEGER, rel TEXT, doc_id INTEGER,
  PRIMARY KEY(src, dst, rel, doc_id));
CREATE TABLE IF NOT EXISTS interactions(id INTEGER PRIMARY KEY, user TEXT, query TEXT,
  lane TEXT, answer TEXT, confidence REAL, at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS query_cache(qhash TEXT PRIMARY KEY, query TEXT, answer TEXT,
  meta TEXT, at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS expiry_items(id INTEGER PRIMARY KEY, client_id INTEGER,
  kind TEXT, label TEXT, expires TEXT);
CREATE TABLE IF NOT EXISTS feature_requests(id INTEGER PRIMARY KEY, user TEXT, body TEXT,
  priority INTEGER DEFAULT 3, category TEXT, at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS skill_suggestions(id INTEGER PRIMARY KEY, pattern TEXT,
  count INTEGER, params TEXT, at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS imap_state(mailbox TEXT PRIMARY KEY, last_uid INTEGER DEFAULT 0);
CREATE TABLE IF NOT EXISTS browser_workflows(id INTEGER PRIMARY KEY, name TEXT UNIQUE, steps TEXT);
CREATE TABLE IF NOT EXISTS browser_sessions(host TEXT PRIMARY KEY, mode TEXT DEFAULT 'auto',
  failures INTEGER DEFAULT 0);
CREATE TABLE IF NOT EXISTS deadlines(id INTEGER PRIMARY KEY, kind TEXT, rule TEXT,
  description TEXT, UNIQUE(kind));
CREATE TABLE IF NOT EXISTS deadline_dates(id INTEGER PRIMARY KEY, kind TEXT, due TEXT, year INTEGER);
CREATE TABLE IF NOT EXISTS quickref(id INTEGER PRIMARY KEY, key TEXT UNIQUE, label TEXT,
  value TEXT, unit TEXT, category TEXT, source TEXT, keywords TEXT);
CREATE TABLE IF NOT EXISTS dnevnice_rates(country TEXT PRIMARY KEY, amount REAL,
  currency TEXT DEFAULT 'EUR', source TEXT);
CREATE TABLE IF NOT EXISTS notifications(id INTEGER PRIMARY KEY, kind TEXT, body TEXT,
  client_id INTEGER, seen INTEGER DEFAULT 0, at TEXT DEFAULT (datetime('now')));
CREATE TABLE IF NOT EXISTS eracuni(id INTEGER PRIMARY KEY, doc_id INTEGER, supplier_oib TEXT,
  customer_oib TEXT, total REAL, vat REAL, currency TEXT, issued TEXT, raw_path TEXT);
"""

class Spine:
    def __init__(self, db_path: str):
        self.db_path = db_path
        self._local = threading.local()
        self._wlock = threading.Lock()
        with self.write() as c:
            c.executescript(SCHEMA)

    def _conn(self) -> sqlite3.Connection:
        c = getattr(self._local, "conn", None)
        if c is None:
            c = sqlite3.connect(self.db_path, timeout=30)
            c.row_factory = sqlite3.Row
            c.execute("PRAGMA journal_mode=WAL")
            c.execute("PRAGMA busy_timeout=10000")
            self._local.conn = c
        return c

    def read(self) -> sqlite3.Connection:
        return self._conn()

    @contextmanager
    def write(self):
        with self._wlock:
            c = self._conn()
            try:
                yield c; c.commit()
            except Exception:
                c.rollback(); raise

    def get_override(self, module, key, default=None):
        r = self.read().execute("SELECT value FROM config_overrides WHERE module=? AND key=?",
                                (module, key)).fetchone()
        return r["value"] if r else default

    def set_override(self, module, key, value, source_url=""):
        with self.write() as c:
            c.execute("""INSERT INTO config_overrides(module,key,value,source_url,updated_at)
                VALUES(?,?,?,?,datetime('now')) ON CONFLICT(module,key) DO UPDATE SET
                value=excluded.value, source_url=excluded.source_url, updated_at=excluded.updated_at""",
                (module, key, str(value), source_url))

    def audit(self, user, action, entity="", detail=""):
        with self.write() as c:
            c.execute("INSERT INTO audit_log(user,action,entity,detail) VALUES(?,?,?,?)",
                      (user, action, entity, detail))

    def close(self):
        c = getattr(self._local, "conn", None)
        if c: c.close(); self._local.conn = None

_spine: Spine | None = None
def init_spine(db_path: str) -> Spine:
    global _spine; _spine = Spine(db_path); return _spine
def get_spine() -> Spine:
    assert _spine is not None, "init_spine() nije pozvan"
    return _spine
```

Dodaj u `tests/conftest.py`: fixture `spine(tmp_path)` → `Spine(str(tmp_path/"t.db"))`.

- [ ] **Step 4: Run — PASS. Step 5: Commit** `feat: spine — SQLite WAL, schema 35+ tablica, overrides, audit`

### Task 3: core/security.py

**Files:**
- Create: `ragspine/core/security.py`
- Test: `tests/test_security.py`

**Interfaces:**
- Produces: `jwt_encode(payload: dict, secret: str, ttl_s=86400) -> str`, `jwt_decode(token, secret) -> dict` (raise `AuthError` na krivi potpis/istek), `hash_password(pw) -> str` (PBKDF2-HMAC-SHA256, 200k iter, format `salt$hash`), `verify_password(pw, stored) -> bool`, `oib_valid(oib: str) -> bool` (ISO 7064 mod 11,10), `redact_pii(text) -> str` (OIB→`[OIB]`, IBAN→`[IBAN]`, email→`[EMAIL]`, tel→`[TEL]`), `chain_append(spine, event: str) -> str`, `chain_verify(spine) -> bool`, `class AuthError(Exception)`.

- [ ] **Step 1: failing testovi**

```python
# tests/test_security.py
import time, pytest
from ragspine.core import security as sec

def test_jwt_roundtrip():
    t = sec.jwt_encode({"sub": "ana"}, "s3cret")
    assert sec.jwt_decode(t, "s3cret")["sub"] == "ana"

def test_jwt_bad_sig():
    t = sec.jwt_encode({"sub": "ana"}, "s3cret")
    with pytest.raises(sec.AuthError): sec.jwt_decode(t, "drugi")

def test_jwt_expired():
    t = sec.jwt_encode({"sub": "ana"}, "s", ttl_s=-1)
    with pytest.raises(sec.AuthError): sec.jwt_decode(t, "s")

def test_password():
    h = sec.hash_password("lozinka1")
    assert sec.verify_password("lozinka1", h) and not sec.verify_password("x", h)

def test_oib():
    assert sec.oib_valid("69435151530")      # validan testni OIB
    assert not sec.oib_valid("69435151531")
    assert not sec.oib_valid("123")

def test_redact():
    s = sec.redact_pii("OIB 69435151530, mail ana@x.hr, IBAN HR1210010051863000160, tel +385 91 123 4567")
    for tok in ["[OIB]", "[EMAIL]", "[IBAN]", "[TEL]"]: assert tok in s
    assert "69435151530" not in s

def test_chain(spine):
    sec.chain_append(spine, "e1"); sec.chain_append(spine, "e2")
    assert sec.chain_verify(spine)
    with spine.write() as c: c.execute("UPDATE hash_chain SET event='tamper' WHERE id=1")
    assert not sec.chain_verify(spine)
```

- [ ] **Step 2: FAIL. Step 3: Implementacija** — JWT: `base64.urlsafe_b64encode` header `{"alg":"HS256","typ":"JWT"}` + payload s `exp`, potpis `hmac.new(secret, f"{h}.{p}", sha256)`; decode s `hmac.compare_digest`. OIB: mod 11,10 algoritam (kontrolna = (11 - međurezultat) % 10 varijanta ISO 7064). PII regexi: OIB `\b\d{11}\b` (uz oib_valid check), IBAN `\bHR\d{19}\b`, email standard, tel `(\+385|0)[\s\d/-]{7,}`. Chain: `hash = sha256(prev_hash + event)`, genesis prev="".
- [ ] **Step 4: PASS. Step 5: Commit** `feat: security — JWT hmac, PBKDF2, OIB mod11, PII redakcija, hash-chain`

### Task 4: core/subproc.py + core/net.py (SSRF guard)

**Files:**
- Create: `ragspine/core/subproc.py`, `ragspine/core/net.py`
- Test: `tests/test_subproc.py`, `tests/test_net.py`

**Interfaces:**
- Produces: `subproc.run_isolated(cmd: list[str], timeout=60, cwd=None, mem_mb=512) -> (rc, stdout, stderr)` — process group, kill-tree na timeout, RLIMIT_AS na POSIX-u; `net.safe_fetch(url, timeout=20, max_bytes=5_000_000, headers=None) -> bytes` — samo http/https, resolve host pa blokiraj privatne/loopback/link-local IP-ove osim ako je host u `get_config().egress_allow`; raise `net.EgressBlocked`.

- [ ] **Step 1: failing testovi**

```python
# tests/test_subproc.py
import sys, time
from ragspine.core.subproc import run_isolated

def test_ok():
    rc, out, _ = run_isolated([sys.executable, "-c", "print('hi')"])
    assert rc == 0 and out.strip() == "hi"

def test_timeout_kills_tree():
    t0 = time.time()
    rc, _, _ = run_isolated([sys.executable, "-c",
        "import subprocess,sys,time; subprocess.Popen([sys.executable,'-c','import time;time.sleep(60)']); time.sleep(60)"],
        timeout=2)
    assert rc != 0 and time.time() - t0 < 10
```

```python
# tests/test_net.py
import pytest
from ragspine.core.net import safe_fetch, EgressBlocked

@pytest.mark.parametrize("url", ["http://127.0.0.1/x", "http://localhost/x",
                                 "http://192.168.1.1/x", "ftp://porezna.hr/x", "file:///etc/passwd"])
def test_blocked(url, cfg):
    with pytest.raises(EgressBlocked): safe_fetch(url)

def test_allowlist(cfg, monkeypatch):
    cfg.egress_allow.append("127.0.0.1")
    # dalje puca na connection refused, NE na EgressBlocked
    with pytest.raises(OSError): safe_fetch("http://127.0.0.1:1/x", timeout=1)
```

- [ ] **Step 2: FAIL. Step 3: Implementacija** — subproc: `subprocess.Popen(start_new_session=True)` + `preexec_fn` s `resource.setrlimit(RLIMIT_AS)` (POSIX only, `hasattr` guard), na timeout `os.killpg(os.getpgid(pid), SIGKILL)`. net: `urllib.parse` scheme check, `socket.getaddrinfo` pa `ipaddress.ip_address(...).is_private/is_loopback/is_link_local/is_reserved` check svake adrese, zatim `urllib.request.urlopen` s `Request(headers={"User-Agent": "RAGSPINE/1.0"})`, čitaj max `max_bytes`.
- [ ] **Step 4: PASS. Step 5: Commit** `feat: izolirani subprocesi + SSRF-guarded fetch`

## Faza B — API skeleton + CLI

### Task 5: web/api.py skeleton + auth

**Files:**
- Create: `ragspine/web/__init__.py`, `ragspine/web/api.py`, `ragspine/web/deps.py`
- Test: `tests/test_api_auth.py`

**Interfaces:**
- Produces: `api.create_app(spine, cfg) -> FastAPI`. Endpointi: `GET /health` (bez autha, vraća `{"status":"ok","missing":{...}}`), `POST /auth/login` `{username,password}` → `{"token": jwt}`, `GET /v1/models` (OpenAI-compat, traži auth). `deps.require_user` FastAPI dependency: `Authorization: Bearer <jwt>` → username, 401 inače. `add_user(spine, username, password, role='radnik')` helper u deps.
- Svi kasniji routeri koriste `Depends(require_user)`.

- [ ] **Step 1: failing testovi**

```python
# tests/test_api_auth.py
from fastapi.testclient import TestClient
from ragspine.web.api import create_app
from ragspine.web.deps import add_user

def _client(spine, cfg):
    return TestClient(create_app(spine, cfg))

def test_health_no_auth(spine, cfg):
    r = _client(spine, cfg).get("/health")
    assert r.status_code == 200 and r.json()["status"] == "ok"

def test_login_and_protected(spine, cfg):
    add_user(spine, "ana", "tajna")
    c = _client(spine, cfg)
    assert c.get("/v1/models").status_code == 401
    tok = c.post("/auth/login", json={"username": "ana", "password": "tajna"}).json()["token"]
    r = c.get("/v1/models", headers={"Authorization": f"Bearer {tok}"})
    assert r.status_code == 200 and r.json()["object"] == "list"

def test_bad_login(spine, cfg):
    add_user(spine, "ana", "tajna")
    assert _client(spine, cfg).post("/auth/login",
        json={"username": "ana", "password": "x"}).status_code == 401
```

- [ ] **Step 2: FAIL. Step 3: Implementacija** — `create_app` sprema spine/cfg u `app.state`, include_router za auth; `require_user` čita header, `jwt_decode`, vraća `sub`. `/v1/models` vraća `{"object":"list","data":[{"id": cfg.llm_model or "ragspine", "object":"model"}]}`. Login: SELECT users, `verify_password`, `jwt_encode({"sub": username, "role": role}, cfg.jwt_secret)`.
- [ ] **Step 4: PASS. Step 5: Commit** `feat: FastAPI skeleton — health, JWT login, /v1/models`

### Task 6: CLI (__main__.py)

**Files:**
- Create: `ragspine/__main__.py`
- Test: `tests/test_cli.py`

**Interfaces:**
- Produces: `python -m ragspine <cmd>`; subkomande: `serve`, `setup`, `doctor`, `health`, `ingest <path>`, `forget <term>`, `eval`, `stats`, `reminders`, `auth add <user>` (lozinka iz `RAGSPINE_PASS` env ili prompt), `browser <sub>`, `watch run`, `ocr <path>`. `main(argv=None) -> int`. Komande koje ovise o kasnijim taskovima registriraju se odmah, dispatch na funkcije koje kasniji taskovi pune (lazy import unutar handlera; do tada handler ispiše "nije još implementirano" i vrati 2 — svaki kasniji task briše svoj stub).

- [ ] **Step 1: failing test**

```python
# tests/test_cli.py
from ragspine.__main__ import main

def test_auth_add_and_doctor(tmp_path, monkeypatch, capsys):
    monkeypatch.setenv("RAGSPINE_DATA_DIR", str(tmp_path))
    monkeypatch.setenv("RAGSPINE_PASS", "tajna123")
    assert main(["auth", "add", "ana"]) == 0
    out = capsys.readouterr().out
    assert "ana" in out

def test_unknown_cmd():
    assert main(["nepostojece"]) == 2
```

- [ ] **Step 2: FAIL. Step 3: Implementacija** — argparse subparsers; `auth add` → init_spine(cfg.db_path) + `add_user`; `serve` → uvicorn.run(create_app(...)). Step 4: PASS. **Step 5: Commit** `feat: CLI skeleton`

## Faza C — ingest + retrieval

### Task 7: docs/ingest.py

**Files:**
- Create: `ragspine/docs/__init__.py`, `ragspine/docs/ingest.py`
- Test: `tests/test_ingest.py`

**Interfaces:**
- Produces: `chunk_text(text: str, size=1200, overlap=150) -> list[str]` (reže po odlomcima/rečenicama, ne siječe riječi), `detect_doc_type(text, filename="") -> str` (`racun|ugovor|bilanca|zakon|sop|ostalo` po ključnim riječima), `extract_text(path) -> str` (ladder: .pdf→PyMuPDF opt, .docx→python-docx opt, .xlsx→openpyxl opt, .txt/.md→direktno; nepodržano → `UnsupportedFormat`), `ingest_text(spine, text, title, doc_type=None, client_id=None, source_url="", path="") -> int|None` (None ako dedup po sha256), `ingest_file(spine, path, client_id=None) -> int|None`, `bulk_ingest(spine, folder) -> dict` (`{"ingested": n, "skipped": m, "errors": [...]}`).
- Nakon inserta chunkova poziva `rag.embed.index_chunks(spine, chunk_ids)` ako je embed dostupan (lazy import, try/except ImportError dok Task 8 ne postoji → guard `optional`).

- [ ] **Step 1: failing testovi**

```python
# tests/test_ingest.py
from ragspine.docs import ingest as ing

def test_chunker_no_word_split():
    text = " ".join(["riječ%d" % i for i in range(2000)])
    chunks = ing.chunk_text(text, size=500, overlap=50)
    assert all(len(c) <= 600 for c in chunks)
    assert all(not c.startswith(" ") for c in chunks)
    joined = " ".join(chunks)
    assert "riječ1999" in joined

def test_detect_type():
    assert ing.detect_doc_type("Račun br. 55/2026, PDV 25%") == "racun"
    assert ing.detect_doc_type("Članak 1. Ovim se Zakonom uređuje...") == "zakon"
    assert ing.detect_doc_type("bla") == "ostalo"

def test_ingest_and_dedup(spine):
    d1 = ing.ingest_text(spine, "Zakon o PDV-u, stopa 25%.", "zakon-pdv")
    assert d1 is not None
    assert ing.ingest_text(spine, "Zakon o PDV-u, stopa 25%.", "zakon-pdv") is None
    n = spine.read().execute("SELECT COUNT(*) FROM chunks WHERE doc_id=?", (d1,)).fetchone()[0]
    assert n >= 1

def test_ingest_txt_file(spine, tmp_path):
    p = tmp_path / "a.txt"; p.write_text("Ugovor o radu sklopljen...")
    assert ing.ingest_file(spine, str(p)) is not None

def test_bulk(spine, tmp_path):
    (tmp_path / "a.txt").write_text("dokument A" * 50)
    (tmp_path / "b.md").write_text("dokument B" * 50)
    r = ing.bulk_ingest(spine, str(tmp_path))
    assert r["ingested"] == 2
```

- [ ] **Step 2: FAIL. Step 3: Implementacija** — chunker: split po `\n\n` pa akumuliraj do size, overlap zadnjih ~overlap znakova na granici riječi. detect: dict ključnih riječi po tipu, max score, tie→ostalo. sha256 nad normaliziranim tekstom. Step 4: PASS. **Step 5: Commit** `feat: ingest — parser ladder, chunker, dedup, bulk`

### Task 8: rag/embed.py + rag/retrieval.py

**Files:**
- Create: `ragspine/rag/__init__.py`, `ragspine/rag/embed.py`, `ragspine/rag/retrieval.py`
- Test: `tests/test_retrieval.py`

**Interfaces:**
- Produces: `embed.available() -> bool` (fastembed I sqlite_vec prisutni), `embed.ensure_vec_table(spine)`, `embed.index_chunks(spine, chunk_ids: list[int])`, `embed.query_vec(spine, query, k) -> list[(chunk_id, dist)]`; `retrieval.Hit` dataclass `(chunk_id, doc_id, title, text, score, doc_type)`, `retrieval.search(spine, query, k=8, freshness=True) -> list[Hit]` (FTS5 + vec ako available + RRF; freshness: preskoči `stale=1` dokumente i one s `valid_until < date('now')`), `retrieval.rrf(rank_lists: list[list[int]], k=60) -> dict[int, float]`.
- FTS query: sanitiziraj user input (escape navodnika, spoji tokene s OR) da sirovi upit ne sruši FTS parser.

- [ ] **Step 1: failing testovi**

```python
# tests/test_retrieval.py
from ragspine.docs import ingest as ing
from ragspine.rag import retrieval

def _seed(spine):
    ing.ingest_text(spine, "Stopa PDV-a u Hrvatskoj je 25 posto, snižena 13 i 5.", "pdv-stope", doc_type="zakon")
    ing.ingest_text(spine, "Ugovor o najmu poslovnog prostora u Splitu.", "najam", doc_type="ugovor")
    ing.ingest_text(spine, "Minimalna plaća za 2026. iznosi X eura bruto.", "minimalac", doc_type="zakon")

def test_fts_search(spine):
    _seed(spine)
    hits = retrieval.search(spine, "kolika je stopa PDV-a?")
    assert hits and hits[0].title == "pdv-stope"

def test_rrf_fusion():
    fused = retrieval.rrf([[1, 2, 3], [2, 1, 4]])
    assert fused[2] > fused[3] and fused[1] > fused[4]

def test_freshness_excludes_stale(spine):
    _seed(spine)
    with spine.write() as c: c.execute("UPDATE documents SET stale=1 WHERE title='pdv-stope'")
    hits = retrieval.search(spine, "stopa PDV")
    assert all(h.title != "pdv-stope" for h in hits)

def test_hostile_fts_input(spine):
    _seed(spine)
    retrieval.search(spine, 'pdv" OR x(')  # ne smije raisati
```

- [ ] **Step 2: FAIL. Step 3: Implementacija** — FTS: tokeni `re.findall(r"\w+", q)`, svaki u navodnike, join ` OR `, `SELECT rowid, rank FROM chunks_fts WHERE chunks_fts MATCH ? ORDER BY rank LIMIT 30`. Vec: fastembed `TextEmbedding(cfg.embed_model)` lazy singleton, sqlite-vec `vec0` virtualna tablica `vec_chunks(chunk_id INTEGER PRIMARY KEY, embedding float[1024])`. RRF: `score[d] += 1/(k + rank)`. JOIN chunks+documents za Hit polja + freshness WHERE. Step 4: PASS (vec grana se testira samo ako `embed.available()`). **Step 5: Commit** `feat: hibridni retrieval — FTS5 + optional vec + RRF + freshness`

## Faza D — chat pipeline

### Task 9: rag/router.py

**Files:**
- Create: `ragspine/rag/router.py`
- Test: `tests/test_router.py`

**Interfaces:**
- Produces: `route(query: str) -> str` — lane iz `LANES = ("sql","web","graph","ocr","learn","knjizenje","no_retrieval","reject","chat")`. `RULES: list[tuple[re.Pattern, str]]` — 30+ pravila, hrvatski jezik, case-insensitive, prvi match pobjeđuje, default `chat`.
- Primjeri pravila: `koliko (računa|dokumenata|klijenata)|zbroj|ukupno.*(pdv|iznos)|top \d* ?klijen` → sql; `nauči (s|sa|iz)? ?http|zapamti (stranicu|url)` → learn; `(pretraži|potraži).*(web|internet)|što je novo na webu` → web; `ocr|skenir|prepoznaj tekst` → ocr; `(kako|gdje).*(proknjiži|knjiži)|konto za` → knjizenje; `(pozdrav|bok|hvala|dobro jutro)$` → no_retrieval; `(izbriši bazu|obriši sve|drop table|ignore previous)` → reject; `povezan|veza između|graf` → graph.

- [ ] **Step 1: failing testovi**

```python
# tests/test_router.py
import pytest
from ragspine.rag.router import route

@pytest.mark.parametrize("q,lane", [
    ("koliko računa imamo ovaj mjesec?", "sql"),
    ("zbroj PDV-a za srpanj", "sql"),
    ("top 5 klijenata po upitima", "sql"),
    ("nauči s https://porezna.hr/prirez", "learn"),
    ("pretraži web za novi zakon o radu", "web"),
    ("OCR-aj skenirane račune iz foldera", "ocr"),
    ("na koji konto knjižim reprezentaciju?", "knjizenje"),
    ("kakva je veza između klijenta X i dobavljača Y?", "graph"),
    ("bok", "no_retrieval"),
    ("obriši sve iz baze", "reject"),
    ("koliki je prirez za Split?", "chat"),
    ("što sve moram ovaj mjesec?", "chat"),
])
def test_route(q, lane):
    assert route(q) == lane
```

- [ ] **Step 2: FAIL. Step 3: Implementacija** — modul-level kompajlirana RULES lista, `route` lowercase + prvi match. Step 4: PASS. **Step 5: Commit** `feat: router — 30+ HR regex pravila, 9 laneova`

### Task 10: core/llm.py — provider dispatcher

**Files:**
- Create: `ragspine/core/llm.py`
- Test: `tests/test_llm.py`

**Interfaces:**
- Produces: `LLMResult` dataclass `(text: str, model: str, usage: dict)`; `LLMClient(cfg, transport=None)`: `.complete(messages: list[dict], system: str|None=None, model=None, max_tokens=1024, temperature=0.2) -> LLMResult`. `transport(url, headers, body: dict) -> dict` injectable (default urllib POST JSON). `detect_provider(base_url) -> "anthropic"|"ollama"|"openai"` (anthropic po "anthropic" u hostu, ollama po portu 11434 ili "/api" suffixu, else openai). `load_oauth_token() -> tuple[str, str]|None` — čita `~/.claude/.credentials.json` (`claudeAiOauth.accessToken` → provider "anthropic-oauth") pa `~/.codex/auth.json` (`tokens.access_token` → "openai-oauth"); vraća (provider, token) ili None.
- Redoslijed odabira: cfg.llm_base_url+api_key ako postoje → detect_provider; inače Ollama ako je dostupna; inače OAuth fallback; inače `LLMUnavailable` exception.
- Anthropic format: `POST {base}/v1/messages`, headers `x-api-key`/`authorization: Bearer` (OAuth) + `anthropic-version: 2023-06-01`; body `{model, max_tokens, system, messages}`; text iz `content[0].text`. OpenAI format: `POST {base}/v1/chat/completions`, system kao prva message; text iz `choices[0].message.content`. Ollama: OpenAI-compat na `{ollama_url}/v1/chat/completions`.

- [ ] **Step 1: failing testovi**

```python
# tests/test_llm.py
import pytest
from ragspine.core.llm import LLMClient, detect_provider, LLMUnavailable

def test_detect():
    assert detect_provider("https://api.anthropic.com") == "anthropic"
    assert detect_provider("http://127.0.0.1:11434/v1") == "ollama"
    assert detect_provider("https://api.deepseek.com") == "openai"

def _fake_openai(url, headers, body):
    assert url.endswith("/v1/chat/completions")
    return {"choices": [{"message": {"content": "odgovor"}}], "model": body["model"], "usage": {}}

def _fake_anthropic(url, headers, body):
    assert url.endswith("/v1/messages") and "anthropic-version" in headers
    return {"content": [{"type": "text", "text": "odgovor"}], "model": body["model"], "usage": {}}

def test_openai_path(cfg):
    cfg.llm_base_url = "https://api.deepseek.com"; cfg.llm_api_key = "k"; cfg.llm_model = "deepseek-chat"
    r = LLMClient(cfg, transport=_fake_openai).complete([{"role": "user", "content": "hej"}])
    assert r.text == "odgovor"

def test_anthropic_path(cfg):
    cfg.llm_base_url = "https://api.anthropic.com"; cfg.llm_api_key = "k"; cfg.llm_model = "claude-sonnet-5"
    r = LLMClient(cfg, transport=_fake_anthropic).complete(
        [{"role": "user", "content": "hej"}], system="ti si knjigovođa")
    assert r.text == "odgovor"

def test_unavailable(cfg, monkeypatch):
    monkeypatch.setattr("ragspine.core.llm.load_oauth_token", lambda: None)
    monkeypatch.setattr("ragspine.core.llm._ollama_alive", lambda cfg: False)
    with pytest.raises(LLMUnavailable):
        LLMClient(cfg).complete([{"role": "user", "content": "x"}])
```

- [ ] **Step 2: FAIL. Step 3: Implementacija** — `_ollama_alive(cfg)`: urllib GET `{ollama_url}/api/tags` timeout 2, bool. Transport default: `urllib.request` POST, JSON, timeout 120, `json.loads` odgovora; HTTP error → `LLMError` s tijelom. Step 4: PASS. **Step 5: Commit** `feat: LLM dispatcher — OpenAI/Anthropic/Ollama + OAuth fallback`

### Task 11: rag/composer.py + rag/citations.py

**Files:**
- Create: `ragspine/rag/composer.py`, `ragspine/rag/citations.py`
- Test: `tests/test_composer.py`, `tests/test_citations.py`

**Interfaces:**
- Produces: `composer.compose(query, hits: list[Hit], extra: str = "") -> (system: str, messages: list[dict])` — system prompt (HR knjigovodstveni asistent, citiraj `[n]`, bez izmišljanja, "ne znam" ako izvori ne pokrivaju), izvori numerirani `[1] (ZAKON) pdv-stope: tekst...` s oznakom tipa (`zakon→ZAKON, racun→ERAČUN, sop→SOP, kontni→KONTNI, ostalo→DOK`); `citations.verify(answer: str, hits) -> Report` dataclass `(cited: list[int], confidence: float, ok: bool)` — parsira `[n]` reference, valjane su 1..len(hits); confidence = pokrivenost (udio odgovora uz citate) × validnost; `ok=False` ako nema valjanih citata a hits postoje; `citations.IDK = "Ne znam — nemam izvor za ovo u bazi."`

- [ ] **Step 1: failing testovi**

```python
# tests/test_citations.py
from ragspine.rag.citations import verify
from ragspine.rag.retrieval import Hit

H = [Hit(1, 1, "pdv", "Stopa PDV-a je 25%.", 1.0, "zakon"),
     Hit(2, 2, "min", "Minimalac je 970 EUR.", 0.9, "zakon")]

def test_valid_citations():
    r = verify("Stopa PDV-a je 25% [1], a minimalac 970 EUR [2].", H)
    assert r.ok and set(r.cited) == {1, 2} and r.confidence > 0.5

def test_no_citations_not_ok():
    r = verify("Stopa je 25%.", H)
    assert not r.ok

def test_out_of_range_ignored():
    r = verify("Nešto [7].", H)
    assert not r.ok and r.cited == []

def test_no_hits_no_requirement():
    r = verify("Pozdrav!", [])
    assert r.ok
```

```python
# tests/test_composer.py
from ragspine.rag.composer import compose
from ragspine.rag.retrieval import Hit

def test_compose_marks_sources():
    sys_p, msgs = compose("stopa pdv?", [Hit(1, 1, "pdv-stope", "Stopa je 25%.", 1.0, "zakon")])
    assert "[1] (ZAKON) pdv-stope" in msgs[-1]["content"]
    assert "ne znam" in sys_p.lower() or "Ne znam" in sys_p
    assert msgs[-1]["role"] == "user"
```

- [ ] **Step 2: FAIL. Step 3: Implementacija. Step 4: PASS. Step 5: Commit** `feat: composer + citations gate`

### Task 12: rag/cache.py + knowledge/kb.py

**Files:**
- Create: `ragspine/rag/cache.py`, `ragspine/knowledge/__init__.py`, `ragspine/knowledge/kb.py`
- Test: `tests/test_cache.py`, `tests/test_kb.py`

**Interfaces:**
- Produces: `cache.get(spine, query) -> str|None` (normalizirani hash, TTL 24h), `cache.put(spine, query, answer, meta="")`; `kb.save(spine, question, answer, category="", tags="") -> int`, `kb.lookup(spine, question, threshold=0.6) -> str|None` (difflib `SequenceMatcher.ratio()` nad normaliziranim pitanjima, increment hits), `kb.list_all(spine, category=None)`.

- [ ] **Step 1: failing testovi**

```python
# tests/test_cache.py
from ragspine.rag import cache

def test_roundtrip_and_normalize(spine):
    cache.put(spine, "Koliki je PDV?", "25% [1]")
    assert cache.get(spine, "koliki je  pdv?") == "25% [1]"

def test_expired(spine):
    cache.put(spine, "q", "a")
    with spine.write() as c:
        c.execute("UPDATE query_cache SET at=datetime('now','-25 hours')")
    assert cache.get(spine, "q") is None
```

```python
# tests/test_kb.py
from ragspine.knowledge import kb

def test_similar_lookup(spine):
    kb.save(spine, "Koliki je prag za ulazak u sustav PDV-a?", "60.000 EUR", "pdv")
    assert kb.lookup(spine, "koji je prag za ulaz u pdv sustav?") == "60.000 EUR"
    assert kb.lookup(spine, "kako se knjiži amortizacija?") is None
```

- [ ] **Step 2-4: FAIL → impl → PASS. Step 5: Commit** `feat: query cache 24h + knowledge base difflib`

### Task 13: rag/pipeline.py + chat endpointi

**Files:**
- Create: `ragspine/rag/pipeline.py`
- Modify: `ragspine/web/api.py` (dodaj chat router)
- Test: `tests/test_pipeline.py`

**Interfaces:**
- Produces: `pipeline.answer(spine, cfg, query: str, user: str, llm: LLMClient|None=None) -> dict` `{"answer", "lane", "confidence", "sources": [{"n","title","doc_id"}], "cached": bool}`. Tok: route → reject/no_retrieval kratki spoj → cache.get → kb.lookup → lane dispatch (sql/learn/web/graph/ocr laneovi se dodaju u kasnijim taskovima kroz registry `LANE_HANDLERS: dict[str, callable]`; nepoznat lane pada na chat) → chat lane: retrieval.search → compose → llm.complete → citations.verify (not ok → `citations.IDK`, confidence 0) → cache.put + interactions insert + kb.save (samo ok odgovori).
- API: `POST /chat {q} -> pipeline.answer(...)`; `POST /v1/chat/completions` (OpenAI-compat: uzmi zadnju user poruku, vrati `{"choices":[{"message":{"role":"assistant","content":ans}}],...}`) — Open WebUI kompatibilnost.

- [ ] **Step 1: failing testovi**

```python
# tests/test_pipeline.py
from ragspine.rag import pipeline
from ragspine.docs import ingest as ing
from ragspine.core.llm import LLMClient

def _llm(cfg, text):
    cfg.llm_base_url = "https://api.x.com"; cfg.llm_api_key = "k"; cfg.llm_model = "m"
    return LLMClient(cfg, transport=lambda u, h, b: {
        "choices": [{"message": {"content": text}}], "model": "m", "usage": {}})

def test_chat_with_citation(spine, cfg):
    ing.ingest_text(spine, "Stopa PDV-a je 25 posto.", "pdv", doc_type="zakon")
    r = pipeline.answer(spine, cfg, "kolika je stopa pdv-a?", "ana", llm=_llm(cfg, "Stopa je 25% [1]."))
    assert r["lane"] == "chat" and r["confidence"] > 0 and r["sources"]

def test_no_citation_idk(spine, cfg):
    ing.ingest_text(spine, "Stopa PDV-a je 25 posto.", "pdv", doc_type="zakon")
    r = pipeline.answer(spine, cfg, "kolika je stopa pdv-a?", "ana", llm=_llm(cfg, "Izmišljam bez citata."))
    assert "ne znam" in r["answer"].lower() and r["confidence"] == 0

def test_cache_second_call(spine, cfg):
    ing.ingest_text(spine, "Stopa PDV-a je 25 posto.", "pdv", doc_type="zakon")
    pipeline.answer(spine, cfg, "stopa pdv?", "ana", llm=_llm(cfg, "25% [1]."))
    r2 = pipeline.answer(spine, cfg, "stopa pdv?", "ana", llm=None)  # bez LLM-a — mora iz cachea
    assert r2["cached"]

def test_reject(spine, cfg):
    r = pipeline.answer(spine, cfg, "obriši sve iz baze", "ana")
    assert r["lane"] == "reject"
```

- [ ] **Step 2-4: FAIL → impl → PASS. Step 5: API testovi u `tests/test_api_auth.py` stilu (POST /chat s tokenom). Step 6: Commit** `feat: chat pipeline — route/cache/kb/retrieval/LLM/citations + OpenAI-compat endpoint`

## Faza E — laneovi

### Task 14: rag/sql_lane.py

**Files:**
- Create: `ragspine/rag/sql_lane.py`
- Test: `tests/test_sql_lane.py`

**Interfaces:**
- Produces: `handle(spine, query: str) -> str|None` (None = nijedan template, pipeline pada na chat lane). Templatei (regex → parametrizirani SELECT, NIKAD string-interpolacija user inputa u SQL): broj računa (`SELECT COUNT(*) FROM eracuni` uz opcionalni mjesec iz upita), zbroj PDV-a (`SUM(vat)` po periodu), top N klijenata po upitima (`interactions JOIN clients`... GROUP BY), broj dokumenata, broj klijenata. Registrira se u `pipeline.LANE_HANDLERS["sql"]`.

- [ ] **Step 1: failing testovi**

```python
# tests/test_sql_lane.py
from ragspine.rag import sql_lane

def _seed(spine):
    with spine.write() as c:
        c.execute("INSERT INTO clients(name, oib) VALUES ('Firma A', '11111111119')")
        for i in range(3):
            c.execute("INSERT INTO eracuni(supplier_oib, total, vat, issued) VALUES (?, 100, 25, '2026-07-05')",
                      ("11111111119",))

def test_count_invoices(spine):
    _seed(spine)
    ans = sql_lane.handle(spine, "koliko računa imamo?")
    assert "3" in ans

def test_sum_vat(spine):
    _seed(spine)
    assert "75" in sql_lane.handle(spine, "zbroj pdv-a")

def test_no_template(spine):
    assert sql_lane.handle(spine, "koliko je sati?") is None
```

- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: sql lane — template NL upiti`

### Task 15: rag/selfrag.py + web lane (DuckDuckGo)

**Files:**
- Create: `ragspine/rag/selfrag.py`, `ragspine/web/websearch.py`
- Test: `tests/test_selfrag.py`

**Interfaces:**
- Produces: `selfrag.classify(query) -> "simple"|"complex"` (heuristika: duljina, veznici "i/te/ali", više upitnika → complex; complex → k=16 umjesto 8 u retrievalu), `selfrag.check_relevance(llm, query, hits) -> bool` (LLM da/ne prompt; bez LLM-a → True), `websearch.ddg(query, fetch=safe_fetch) -> list[dict]` (`{"title","url","snippet"}` — DDG html endpoint `https://html.duckduckgo.com/html/?q=`, parse `html.parser`om), `websearch.handle(spine, cfg, query, llm) -> str` (web lane handler: ddg → compose s [WEB] izvorima → LLM). Pipeline chat lane: ako `check_relevance` False → web fallback.

- [ ] **Step 1: failing testovi** — `classify` parametrizirano; `ddg` s fake fetchom koji vraća snimljeni minimalni DDG HTML fixture (inline bytes u testu, 2 rezultata) → assert parsirana 2 rezultata s url+title.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: self-RAG — kompleksnost, relevance check, DDG web fallback`

### Task 16: rag/graphrag.py

**Files:**
- Create: `ragspine/rag/graphrag.py`
- Test: `tests/test_graphrag.py`

**Interfaces:**
- Produces: `extract_entities(text) -> list[tuple[str, str]]` (`("oib", "...")` validirani OIB-ovi, `("konto", "4xxx")`, `("datum", "1.1.2027")`, `("iznos", "1.234,56 EUR")`, `("zakon", "Zakon o PDV-u")` — regexi), `index_doc(spine, doc_id, text)` (upsert kg_nodes + kg_edges `mentions`/`co_occurs`), `traverse(spine, seeds: list[int], hops=2) -> set[int]` (node idjevi), `handle(spine, cfg, query, llm) -> str` (graph lane: entiteti iz upita → traverse → doc_id-jevi → chunks → compose → LLM). Ingest (Task 7) poziva `index_doc` nakon inserta (lazy import + try/except).

- [ ] **Step 1: failing testovi**

```python
# tests/test_graphrag.py
from ragspine.rag import graphrag as g

def test_extract():
    ents = dict(g.extract_entities("Klijent OIB 69435151530, konto 4010, iznos 1.500,00 EUR"))
    assert ents["oib"] == "69435151530" and ents["konto"] == "4010"

def test_index_and_traverse(spine):
    g.index_doc(spine, 1, "OIB 69435151530 konto 4010")
    g.index_doc(spine, 2, "OIB 69435151530 konto 7500")
    n = spine.read().execute("SELECT id FROM kg_nodes WHERE kind='oib'").fetchone()["id"]
    reach = g.traverse(spine, [n], hops=2)
    kinds = {spine.read().execute("SELECT kind FROM kg_nodes WHERE id=?", (i,)).fetchone()["kind"] for i in reach}
    assert "konto" in kinds
```

- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: graph RAG — entiteti, kg_edges, multi-hop`

## Faza F — watchlist + learn

### Task 17: web/watchlist.py — core

**Files:**
- Create: `ragspine/web/watchlist.py`
- Test: `tests/test_watchlist.py`

**Interfaces:**
- Produces: `add_source(spine, url, category="", client_id=None, added_by="", kind="page") -> int`, `check_source(spine, cfg, source_row, fetch=safe_fetch) -> Change|None`, `check_all(spine, cfg, fetch=safe_fetch) -> list[Change]`; `Change` dataclass `(source_id, url, summary, diff: list[str])`. Logika: fetch → strip HTML u tekst → sha256 vs watch_state.last_hash → promjena: spremi law_versions, `law_diff(old, new) -> list[str]` (unified diff linija s brojkama, format `"Članak/linija: staro → novo"` po linijama koje sadrže brojke), ekstrakcija brojki `extract_rates(text) -> dict` (grad+postotak parovi) → `set_override("kalkulator", f"prirez.{grad}", stopa, url)`, `extract_effective_dates(text) -> list[(desc, date)]` ("stupa na snagu 1.1.2027.") → upcoming_changes, notifications insert, ingest stranice kao dokument. `mark_stale(spine)`: dokumenti s `valid_until < now` → stale=1. API: `POST /watchlist/run`, `GET/POST /watchlist/sources`.

- [ ] **Step 1: failing testovi**

```python
# tests/test_watchlist.py
from ragspine.web import watchlist as w

HTML1 = b"<html><body>Prirez Split iznosi 10%. Stupa na snagu 1.1.2027.</body></html>"
HTML2 = b"<html><body>Prirez Split iznosi 12%. Stupa na snagu 1.1.2027.</body></html>"

def test_first_check_stores_no_change(spine, cfg):
    sid = w.add_source(spine, "https://porezna.example/prirez")
    ch = w.check_source(spine, cfg, w.get_source(spine, sid), fetch=lambda u, **k: HTML1)
    assert ch is None  # prvi fetch = baseline

def test_change_detected_and_override(spine, cfg):
    sid = w.add_source(spine, "https://porezna.example/prirez")
    w.check_source(spine, cfg, w.get_source(spine, sid), fetch=lambda u, **k: HTML1)
    ch = w.check_source(spine, cfg, w.get_source(spine, sid), fetch=lambda u, **k: HTML2)
    assert ch is not None and any("10" in d and "12" in d for d in ch.diff)
    assert spine.get_override("kalkulator", "prirez.Split") == "12"
    up = spine.read().execute("SELECT * FROM upcoming_changes").fetchone()
    assert up["effective_date"] == "2027-01-01"

def test_notification_created(spine, cfg):
    sid = w.add_source(spine, "https://x.example/z")
    w.check_source(spine, cfg, w.get_source(spine, sid), fetch=lambda u, **k: HTML1)
    w.check_source(spine, cfg, w.get_source(spine, sid), fetch=lambda u, **k: HTML2)
    assert spine.read().execute("SELECT COUNT(*) FROM notifications").fetchone()[0] >= 1
```

- [ ] **Step 2-4: TDD ciklus. `get_source(spine, sid) -> Row` helper. Step 5: Commit** `feat: watchlist — hash diff, law diff, overrides, upcoming changes`

### Task 18: watchlist RSS (Narodne novine)

**Files:**
- Modify: `ragspine/web/watchlist.py`
- Test: `tests/test_rss.py`

**Interfaces:**
- Produces: `INDUSTRY_KEYWORDS: dict[str, list[str]]` — 8 industrija (`ugostiteljstvo, gradevina, trgovina, prijevoz, poljoprivreda, it, turizam, proizvodnja`), svaka 5+ HR ključnih riječi; `parse_rss(xml_bytes) -> list[dict]` (`{"title","link","description","date"}` — stdlib `xml.etree`, radi za RSS2.0); `check_rss(spine, cfg, fetch=safe_fetch) -> list[dict]`: za svaki `watch_sources WHERE kind='rss'` fetch+parse, novi itemi (link nije viđen — koristi watch_state.last_content kao JSON set viđenih linkova), keyword match protiv industrija klijenata u bazi (`clients.industry`), samo relevantni → notifications + return. `DEFAULT_RSS = 3 NN feeda` (službeni, međunarodni, oglasi) — seed u Task 36.

- [ ] **Step 1: failing test** — fixture RSS XML (2 itema: "Zakon o ugostiteljskoj djelatnosti" + "Pravilnik o šumarstvu"), klijent s `industry='ugostiteljstvo'` → vrati samo prvi item, notifications=1. Drugi poziv s istim XML-om → 0 novih.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: NN RSS s industry keyword matchingom`

### Task 19: web/learn.py

**Files:**
- Create: `ragspine/web/learn.py`
- Test: `tests/test_learn.py`

**Interfaces:**
- Produces: `HR_GRADOVI: list[str]` (27 gradova: Zagreb, Split, Rijeka, Osijek, Zadar, Pula, Sisak, Karlovac, Varaždin, Šibenik, Dubrovnik, Slavonski Brod, Vinkovci, Vukovar, Bjelovar, Koprivnica, Križevci, Đakovo, Požega, Čakovec, Virovitica, Samobor, Velika Gorica, Kaštela, Solin, Metković, Makarska); `clean_html(html: bytes) -> str` (html.parser, drop script/style); `extract_city_rates(text) -> dict[str, str]` (grad blizu postotka, ±80 znakova prozor); `learn_url(spine, cfg, url, user, fetch=safe_fetch) -> dict` (`{"url","overrides_set": {...},"doc_id"}`): fetch → clean → extract → `set_override("kalkulator", f"prirez.{grad}", ...)` → ingest kao dokument → audit. Registrira `pipeline.LANE_HANDLERS["learn"]` (izvuci URL iz upita regexom; bez URL-a → uputa korisniku).

- [ ] **Step 1: failing test** — fake fetch vraća HTML s "Grad Sisak: stopa prireza 10%" i "Split 15%" → overrides_set ima Sisak=10 i Split=15, doc ingestiran, audit red postoji.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: learn lane — URL → brojke → config_overrides + ingest`

## Faza G — kalkulatori

### Task 20: business/place.py

**Files:**
- Create: `ragspine/business/__init__.py`, `ragspine/business/place.py`
- Test: `tests/test_place.py`

**Interfaces:**
- Produces: `bruto_to_neto(bruto: float, city: str = "", children: int = 0, invalidnost: bool = False, spine=None) -> dict` — ključevi: `bruto, doprinosi (20% MIO), dohodak, odbitak, osnovica, porez, neto, stopa_niza, stopa_visa, detalji: list[str]`. Model 2026: doprinosi iz plaće 20% (MIO I 15 + MIO II 5); osobni odbitak 600 EUR + djeca (1.: 0.5×600, 2.: 0.7×600, 3.: 1.0×600, kumulativno) + invalidnost 0.3×600; porez: niža stopa do 5.000 EUR mjesečne osnovice, viša iznad; stope: override `porez_niza.{city}`/`porez_visa.{city}` → default 20/30; legacy `prirez.{city}` override (postotak na porez) se dodaje ako postoji. `ponytail:` komentar — bez umanjenja MIO za niske plaće i bez neoporezivih primitaka; upgrade path: novi ključevi u overrides.

- [ ] **Step 1: failing testovi**

```python
# tests/test_place.py
from ragspine.business.place import bruto_to_neto

def test_basic_2000():
    r = bruto_to_neto(2000.0)
    assert r["doprinosi"] == 400.0
    assert r["osnovica"] == 1000.0          # 1600 dohodak - 600 odbitak
    assert r["porez"] == 200.0              # 20%
    assert r["neto"] == 1400.0

def test_high_income_upper_band():
    r = bruto_to_neto(10000.0)
    # dohodak 8000, osnovica 7400: 5000*0.2 + 2400*0.3 = 1720
    assert r["porez"] == 1720.0

def test_children_allowance():
    r = bruto_to_neto(2000.0, children=2)
    assert r["odbitak"] == 600 + 300 + 420

def test_city_override(spine):
    spine.set_override("kalkulator", "porez_niza.Split", "21.5")
    r = bruto_to_neto(2000.0, city="Split", spine=spine)
    assert r["stopa_niza"] == 21.5

def test_legacy_prirez_override(spine):
    spine.set_override("kalkulator", "prirez.Sisak", "10")
    r = bruto_to_neto(2000.0, city="Sisak", spine=spine)
    assert r["porez"] == 220.0              # 200 + 10%
```

- [ ] **Step 2-4: TDD ciklus (zaokruživanje na 2 dec). Step 5: Commit** `feat: kalkulator plaće 2026 s override stopama`

### Task 21: business/dnevnice.py + business/quickref.py

**Files:**
- Create: `ragspine/business/dnevnice.py`, `ragspine/business/quickref.py`
- Test: `tests/test_dnevnice.py`, `tests/test_quickref.py`

**Interfaces:**
- Produces: `dnevnice.RATES: dict[str, float]` — 34 države (EU27 + UK, CH, NO, RS, BA, US, domaća "Hrvatska": 30 EUR), defaulti prema Odluci NN (izvor u `SOURCE` konstanti), override ključ `dnevnica.{Država}`; `dnevnice.obracun(country, full_days: int, half_days: int = 0, smjestaj: float = 0, prijevoz: float = 0, spine=None) -> dict` (`{"dnevnica_iznos","ukupno_dnevnice","ukupno"}`); `quickref.SEED: list[dict]` — 24 stavke (`key,label,value,unit,category,source,keywords`): minimalac bruto, PDV prag 60.000, PDV stope 25/13/5, neoporezive nagrade (prigodna 700, radni rezultati 1.120), doprinosi MIO 15/5, zdravstveno 16.5, studentska satnica, dječji doplatak cenzus, otpremnina neoporeziva, božićnica, dar djetetu, terenski dodatak, naknada za odvojeni život, km naknada 0.50, stopa zateznih kamata, obrtnički paušal pragovi (3 razreda), JOPPD rok, PDV rok 20., DOH rok 28.2.; `quickref.seed(spine)` (INSERT OR IGNORE), `quickref.search(spine, term) -> list[Row]` (LIKE po key/label/keywords, vrijednost kroz override `quickref.{key}` ako postoji).

- [ ] **Step 1: failing testovi** — `obracun("Njemačka", 3, 1)` = 3.5 × rate; override `dnevnica.Njemačka=90` mijenja iznos; quickref: seed pa `search(spine, "minimalac")` vraća red s value i source; override mijenja vrijednost.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: dnevnice 34 države + quickref 24 brojke`

## Faza H — poslovni moduli

### Task 22: business/obveze.py + /obveze HTML

**Files:**
- Create: `ragspine/business/obveze.py`, `ragspine/web/templates_obveze.py` (HTML string builder, bez template engine ovisnosti)
- Modify: `ragspine/web/api.py`
- Test: `tests/test_obveze.py`

**Interfaces:**
- Produces: `ensure_period(spine, kind: str, period: str)` — za `kind in ("PDV","JOPPD","DOH")` i period `"2026-07"` kreira obligations za sve aktivne klijente koji tu obvezu imaju (PDV samo `pdv_status LIKE '%u sustavu%'`; JOPPD svi s radnicima — pojednostavljeno: svi aktivni; DOH svi aktivni; `ponytail:` komentar); `list_period(spine, kind, period) -> list[dict]` (`{"client","obligation_id","sent","sent_by","sent_at"}` — neposlani abecedno pa poslani abecedno); `mark_sent(spine, obligation_id, user, sent=True)` + audit. API: `GET /obveze?kind=PDV&period=2026-07` → HTML (neposlani crveni gore, poslani zeleni dolje, gumb → `POST /obveze/mark {obligation_id}` + redirect), `GET /obveze.json`.
- HTML builder: čisti f-string, `html.escape` na SVIM vrijednostima iz baze.

- [ ] **Step 1: failing testovi**

```python
# tests/test_obveze.py
from ragspine.business import obveze

def _seed(spine):
    with spine.write() as c:
        c.execute("INSERT INTO clients(name, oib, pdv_status) VALUES ('Zebra', '1', 'u sustavu pdv')")
        c.execute("INSERT INTO clients(name, oib, pdv_status) VALUES ('Alfa', '2', 'u sustavu pdv')")
        c.execute("INSERT INTO clients(name, oib, pdv_status) VALUES ('Beta', '3', 'nije u pdvu')")

def test_only_pdv_clients(spine):
    _seed(spine)
    obveze.ensure_period(spine, "PDV", "2026-07")
    rows = obveze.list_period(spine, "PDV", "2026-07")
    assert [r["client"] for r in rows] == ["Alfa", "Zebra"]  # Beta van, abecedno

def test_mark_sent_moves_down(spine):
    _seed(spine)
    obveze.ensure_period(spine, "PDV", "2026-07")
    rows = obveze.list_period(spine, "PDV", "2026-07")
    obveze.mark_sent(spine, rows[0]["obligation_id"], "ana")
    rows2 = obveze.list_period(spine, "PDV", "2026-07")
    assert rows2[-1]["client"] == "Alfa" and rows2[-1]["sent"]
```

- [ ] **Step 2-4: TDD + API test (GET /obveze vraća HTML s imenima). Step 5: Commit** `feat: obveze — tablica po klijentu + HTML stranica`

### Task 23: business/kalendar.py + business/expiry.py

**Files:**
- Create: `ragspine/business/kalendar.py`, `ragspine/business/expiry.py`
- Test: `tests/test_kalendar.py`, `tests/test_expiry.py`

**Interfaces:**
- Produces: `kalendar.RULES: list[dict]` — 12 tipova: `{"kind":"PDV","rule":"monthly:20","description":"PDV obrazac do 20. u mjesecu za prethodni"}`; tipovi: PDV (monthly:20), JOPPD (monthly:15 — pojednostavljeno: na dan isplate), DOH (yearly:02-28), PD (yearly:04-30), PDV-S (monthly:20), ZP (monthly:20), OPZ-STAT (quarterly:20), TZ (monthly:_), GFI (yearly:04-30), SR (yearly:02-28), turistička članarina, spomenička renta. Mini-RRULE parser: `expand(rule: str, year: int) -> list[str]` ("monthly:20" → 12 datuma, "yearly:04-30" → 1, "quarterly:20" → 4); `seed(spine, year)` → deadlines + deadline_dates; `upcoming(spine, days=14) -> list[Row]`; `expiry.add(spine, client_id, kind, label, expires)`, `expiry.expiring(spine, days=60) -> list[Row]` (JOIN clients, sortirano po datumu). API: `GET /kalendar?days=`, `GET /expiry?days=`, `POST /expiry`.

- [ ] **Step 1: failing testovi** — `expand("monthly:20", 2026)` = 12 datuma, svi dan 20; `expand("yearly:02-28", 2026)` = ["2026-02-28"]; seed pa `upcoming` s frozen date (monkeypatch `kalendar._today`); expiry: add s datumom za 30 dana → u `expiring(60)`, ne u `expiring(10)`.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: porezni kalendar 12 rokova + praćenje isteka`

### Task 24: business/checklist.py + notes.py + auditlog.py

**Files:**
- Create: `ragspine/business/checklist.py`, `ragspine/business/notes.py`, `ragspine/business/auditlog.py`
- Test: `tests/test_checklist.py`, `tests/test_notes.py`

**Interfaces:**
- Produces: `checklist.score_client(spine, client_id) -> dict` (`{"score": 0-100, "missing": [...]}`) — polja: oib, email, phone, owner, industry, pdv_status (po 10) + dokumenti tipa ugovor (20) + izvod (20istieke — 20); `checklist.worst_first(spine) -> list[dict]`; `notes.add(spine, client_id, author, body) -> int` + audit, `notes.search(spine, term=None, client_id=None) -> list[Row]` (LIKE, kronološki desc); `auditlog.search(spine, client=None, user=None, action=None, limit=100) -> list[Row]`. API: `GET /checklist`, `GET/POST /notes`, `GET /audit`.

- [ ] **Step 1: failing testovi** — klijent samo s imenom → score < 50, "oib" u missing; klijent sa svim poljima + 2 doc-a → score 100; notes add+search po terminu; auditlog.search filtrira po useru.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: checklist kompletnosti + bilješke + audit pretraga`

### Task 25: business/dashboard.py + monthly.py

**Files:**
- Create: `ragspine/business/dashboard.py`, `ragspine/business/monthly.py`
- Test: `tests/test_dashboard.py`

**Interfaces:**
- Produces: `dashboard.stats(spine) -> dict` (`{"active_clients","deadlines_this_week","top_clients": [(name, upiti)], "unseen_notifications"}`); `monthly.overview(spine, period: str) -> dict` — spaja: `deadlines` (kalendar.upcoming za mjesec), `unsent` (obveze.list_period neposlani za PDV/JOPPD/DOH), `expiring` (expiry 45 dana), `watch_changes` (notifications za mjesec), `recent_notes` (zadnjih 10). Odgovara na "što sve moram ovaj mjesec" — registrira se kao poseban intent: `pipeline` prije routera provjerava `monthly.MONTHLY_RE` (`što (sve )?(mi )?(još )?moram|obaveze ovaj mjesec|mjesečni pregled`) → formatiran HR tekst. API: `GET /dashboard`, `GET /monthly?period=`.

- [ ] **Step 1: failing testovi** — seed klijenti+obveze+expiry → `overview` ima sve ključeve, unsent sadrži klijenta; pipeline("što sve moram ovaj mjesec?") vraća lane "monthly" s tekstom koji sadrži ime klijenta.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: dashboard + mjesečni pregled agregator`

## Faza I — dokumenti napredno

### Task 26: docs/eracun.py — UBL 2.1 + auto-sort

**Files:**
- Create: `ragspine/docs/eracun.py`, `tests/fixtures/eracun_ubl.xml`
- Test: `tests/test_eracun.py`

**Interfaces:**
- Produces: `parse_ubl(xml_bytes) -> dict` (`{"supplier_oib","customer_oib","total","vat","currency","issued","lines": [{"name","qty","price"}]}`) — namespaces `cbc`/`cac`, OIB iz `cac:PartyTaxScheme/cbc:CompanyID` (strip `HR` prefiks); `store(spine, parsed, raw_path="") -> int` (eracuni insert + ingest_text sažetka za pretragu); `autosort(spine, cfg, xml_path, pdf_path=None) -> str|None` — customer_oib → `clients.nas_folder` → premjesti XML+PDF (`shutil.move`), path-sanitizacija: destinacija MORA biti unutar `cfg.nas_root` (resolve + `is_relative_to`), klijent nepoznat → None + notifikacija.
- Fixture: minimalni validni UBL 2.1 Invoice (supplier OIB 69435151530, customer OIB 11111111119, 2 stavke, total 125.00, PDV 25.00 EUR).

- [ ] **Step 1: failing testovi** — parse fixture → svi ključevi točni; store → red u eracuni + dokument pretraživ (`retrieval.search "eračun"` pogodi); autosort: klijent s nas_folder u tmp_path → fajl premješten; klijent s `nas_folder="../../etc"` → raise/None (path escape blokiran); nepoznat OIB → None + notifications red.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: e-račun UBL 2.1 parser + OIB auto-sort s path guardom`

### Task 27: docs/imap_fetch.py

**Files:**
- Create: `ragspine/docs/imap_fetch.py`
- Test: `tests/test_imap.py`

**Interfaces:**
- Produces: `fetch_new(spine, cfg, imap=None) -> dict` (`{"fetched": n, "attachments": [paths]}`) — `imaplib.IMAP4_SSL` (injectable fake za test), `UID SEARCH UID {last+1}:*`, privitci `.xml/.pdf` → spremi u `{data_dir}/inbox/`, watermark `imap_state.last_uid` update NAKON uspješnog spremanja; svaki `.xml` pokuša `eracun.autosort`. CLI stub `ingest --imap` poziva ovo.
- Fake IMAP u testu: klasa s `search/fetch` koje vraćaju 2 poruke (jedna s XML privitkom), assert watermark postavljen i drugi poziv vraća 0.

- [ ] **Step 1-4: TDD ciklus. Step 5: Commit** `feat: IMAP fetch s UID watermarkom`

### Task 28: docs/ocr.py

**Files:**
- Create: `ragspine/docs/ocr.py`
- Test: `tests/test_ocr.py`

**Interfaces:**
- Produces: `has_text_layer(path, min_chars=100) -> bool` (PyMuPDF get_text duljina); `rasterize(path, dpi=300) -> list[bytes]` (PNG po stranici); `ocr_page(png: bytes, cfg, transport=None) -> str` — POST na Unlimited-OCR server (OpenAI-compat VLM: `{cfg.ocr_url}/v1/chat/completions`, message s `image_url` data-URI base64 PNG + instrukcija "prepiši sav tekst"); `write_text_layer(path, page_texts: list[str], out_path=None)` — fitz `insert_textbox` s `render_mode=3` (nevidljivo), spremi kao `{stem}_ocr.pdf` ili overwrite uz backup; `ocr_pdf(spine, cfg, path, transport=None) -> dict` (`{"pages": n, "skipped": bool, "out": path}`) — skip ako has_text_layer; `bulk_ocr(spine, cfg, folder) -> dict`. Sve PyMuPDF grane guardane `optional.need("fitz", "OCR/PDF")` — bez njega `OCRUnavailable`. API: `POST /ocr {path}`.
- Test bez PyMuPDF-a: `ocr_page` s fake transportom (vraća `choices[0].message.content`); `has_text_layer`/`write_text_layer` testovi `skipif fitz is None`; s fitz: generiraj 1-stranični PDF s fitz, OCR-aj s fake transportom, write layer, reopen → get_text sadrži OCR tekst.

- [ ] **Step 1-4: TDD ciklus. Step 5: Commit** `feat: OCR — Unlimited-OCR klijent, raster, nevidljivi text layer, bulk skip-if-text`

### Task 29: docs/pdfforms.py + docs/forget.py

**Files:**
- Create: `ragspine/docs/pdfforms.py`, `ragspine/docs/forget.py`
- Test: `tests/test_forget.py`, `tests/test_pdfforms.py`

**Interfaces:**
- Produces: `pdfforms.fill(path, fields: dict, out_path) -> int` (broj popunjenih; fitz widgets, skipif bez fitz); `pdfforms.client_fields(spine, client_id) -> dict` (naziv/OIB/adresa mapping za obrasce); `forget.forget(spine, term: str, dry=False) -> dict` (`{"tablica": broj_obrisanih}`) — sweep SVIH user-data tablica (documents+chunks+chunks_fts kaskadno, clients, notes, eracuni, interactions, knowledge, kg_nodes/edges, audit_log detail match, memory, expiry_items...), zatim `PRAGMA wal_checkpoint(TRUNCATE)`, verifikacija: ponovni sweep count = 0; audit zapis PRIJE brisanja.

- [ ] **Step 1: failing test forget** — seed klijent "Firma X" + dokument s tekstom "Firma X" + interakcija; `forget(spine, "Firma X")` → sve tablice 0 matcheva, `retrieval.search("Firma X")` prazan, dry=True ništa ne briše.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: GDPR forget sweep + PDF AcroForm filler`

## Faza J — knowledge + ops

### Task 30: knowledge/translate.py + features.py + patterns.py

**Files:**
- Create: `ragspine/knowledge/translate.py`, `ragspine/knowledge/features.py`, `ragspine/knowledge/patterns.py`
- Test: `tests/test_knowledge_misc.py`

**Interfaces:**
- Produces: `translate.LANGS = {"hr","en","de","it","sl","fr","es","sr","bs","mk","hu","cs","sk","pl","ru","uk"}`, `translate.translate(llm, text, target: str) -> str` (validira target, LLM prompt); `features.add(spine, user, body, priority=3, category="") -> int`, `features.list_open(spine) -> list[Row]`; `patterns.detect(spine, min_count=5) -> list[dict]` — grupira interactions po normaliziranom obliku (lowercase, brojevi→`#`, difflib buckets), grupa ≥5 → skill_suggestions upsert `{"pattern","count","params"}` (params = koji dijelovi variraju). API: `POST /translate`, `GET/POST /features`.

- [ ] **Step 1: failing testovi** — translate: fake LLM, krivi jezik → ValueError; features add/list; patterns: 6 sličnih interakcija ("koliki je prirez za Split/Sisak/Zadar...") → 1 sugestija s count≥5 i params koji sadrže grad.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: prijevod 16 jezika + feature requests + detekcija obrazaca`

### Task 31: ops/doctor.py + health.py + nis2.py

**Files:**
- Create: `ragspine/ops/__init__.py`, `ragspine/ops/doctor.py`, `ragspine/ops/health.py`, `ragspine/ops/nis2.py`
- Test: `tests/test_ops.py`

**Interfaces:**
- Produces: `doctor.run(cfg) -> list[dict]` (`{"check","ok","detail"}`) — Python≥3.11, disk slobodno ≥1GB, RAM info (`/proc/meminfo` guard), NTP (systemd `timedatectl` ili chrony, subproc, fail→warn), LUKS (`lsblk` grep crypt, warn), Ollama alive, OCR server alive, optional deps (`optional.missing()`), DB writable; `doctor.format_report(results) -> str`; `health.check(spine, cfg) -> dict` (`{"disk_free_mb","wal_size_kb","integrity": "ok"|...}` — `PRAGMA integrity_check`, WAL file size; fail → apprise notify ako dostupan, inače notifications insert); `nis2.CHECKLIST: list[dict]` 12 kontrola (backup, enkripcija, MFA, patch mgmt, logging, incident plan, access control, network seg, vendor mgmt, awareness, BCP, encryption-at-rest), `nis2.report(spine) -> list[dict]` (svaka kontrola + status iz memory tablice `nis2.{id}` override ili "nepoznato"); stubovi `nis2.smart()/lynis()/nmap()` vraćaju `{"status": "stub"}`. CLI `doctor`/`health` pune se ovim (briši stubove iz Task 6).

- [ ] **Step 1: failing testovi** — doctor.run vraća ≥8 checkova, svi imaju ok bool; health.check na spine fixture → integrity "ok"; nis2.report 12 redova.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: doctor preflight + health + NIS2 checklist`

## Faza K — browser

### Task 32: browser/bridge.py + endpointi

**Files:**
- Create: `ragspine/browser/__init__.py`, `ragspine/browser/bridge.py`
- Modify: `ragspine/web/api.py`
- Test: `tests/test_bridge.py`

**Interfaces:**
- Produces: `Bridge()` (singleton u app.state): `.enqueue(cmd: dict) -> str` (cmd_id uuid, queue), `.next_cmd(timeout=25) -> dict|None` (blocking pop za extension long-poll), `.post_result(cmd_id, result: dict)`, `.wait_result(cmd_id, timeout=30) -> dict|None` (threading.Event po cmd_id). API (sve JWT): `GET /browser/cmd` (long-poll, 204 ako ništa), `POST /browser/result {cmd_id, result}`, `POST /browser/run {action, ...} -> result` (enqueue+wait — za API korisnike), `GET /browser/status`.
- Akcije (contract s ekstenzijom): `{"action": "navigate|click|type|scroll|screenshot|read", "selector": "...", "value": "...", "url": "..."}`.

- [ ] **Step 1: failing test** — thread A `enqueue`+`wait_result`, thread B `next_cmd`→`post_result` → A dobije rezultat; timeout bez rezultata → None; API round-trip kroz TestClient.
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: browser bridge — command queue + long-poll endpointi`

### Task 33: browser/workflows.py + sessions.py

**Files:**
- Create: `ragspine/browser/workflows.py`, `ragspine/browser/sessions.py`, `tests/fixtures/recorder.json`
- Test: `tests/test_workflows.py`

**Interfaces:**
- Produces: `workflows.import_recorder(spine, name, recorder_json: dict) -> int` — Chrome DevTools Recorder format (`steps: [{type: navigate|click|change, url/selectors/value}]`) → interni steps JSON; `workflows.parametrize(steps, params: dict) -> list[dict]` (`{{placeholder}}` replace u url/value); `workflows.run(spine, bridge, name, params=None, timeout=60) -> list[dict]` (svaki step → bridge.enqueue+wait, stani na prvi fail); `sessions.mode(spine, host) -> str` ("auto"|"keep"|"immediate", default auto), `sessions.record_failure(spine, host)` — 2 uzastopne greške → mode "keep" + notifications; `sessions.record_success(spine, host)` (reset failures). Fixture: recorder.json s 3 stepa (navigate na `{{url}}`, click, change s `{{oib}}`).

- [ ] **Step 1: failing testovi** — import fixture → workflow u bazi; parametrize zamijeni oba placeholdera; run s fake bridgeom (odmah vraća ok) → 3 rezultata; sessions: 2× record_failure → mode "keep".
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: browser workflows (Recorder import) + session modovi`

### Task 34: browser/agent.py (CDP)

**Files:**
- Create: `ragspine/browser/agent.py`
- Test: `tests/test_agent.py`

**Interfaces:**
- Produces: `available() -> bool` (browser_use importabilan), `run_task(cfg, task: str, url="") -> dict` — browser-use Agent s našim LLMClient wrapperom (browser-use traži langchain-style model; wrappaj `complete` u minimalni objekt s `ainvoke`); bez browser-use → `{"error": "browser-use nije instaliran", "hint": ...}`. API `POST /browser/agent {task, url}`. `ponytail:` v1 = tanki wrapper, bez custom kontrolera.

- [ ] **Step 1: failing test** — bez browser-use instaliranog: `available() is False`, `run_task` vraća error dict s hintom (ne raise). Step 2-4: TDD. **Step 5: Commit** `feat: CDP agent wrapper (optional browser-use)`

### Task 35: extension/

**Files:**
- Create: `extension/manifest.json`, `extension/background.js`, `extension/popup.html`, `extension/popup.js`
- Test: `tests/test_extension.py` (validacija manifesta + JS syntax check node-om ako postoji, inače samo manifest)

**Interfaces:**
- manifest MV3: permissions `["activeTab","scripting","storage","tabs"]`, host_permissions `["<all_urls>"]`, background service_worker. background.js: čita `{server, token}` iz `chrome.storage.local`; petlja: fetch `GET {server}/browser/cmd` (Authorization Bearer; 204 → ponovi), izvrši akciju (navigate: `chrome.tabs.update`; click/type/read: `chrome.scripting.executeScript` s querySelector; screenshot: `chrome.tabs.captureVisibleTab`; scroll: executeScript scrollBy), POST rezultat na `/browser/result`, exponential backoff na greške (1s→30s). popup: form server URL + token → storage, status zadnjeg polla.

- [ ] **Step 1: test** — manifest.json parsira se, `manifest_version == 3`, service_worker postoji kao fajl, popup.html referencira popup.js. Step 2-3: napiši fajlove, test PASS. **Step 4: Commit** `feat: Chrome extension MV3 — polling bridge klijent`

## Faza L — setup + eval + finiš

### Task 36: ops/setup.py — wizard + seedovi

**Files:**
- Create: `ragspine/ops/setup.py`, `ragspine/ops/seeds.py`
- Test: `tests/test_setup.py`

**Interfaces:**
- Produces: `seeds.kontni_plan(spine)` (~45 osnovnih konta RRIF: 0-9 razredi + česta konta: 1000 blagajna, 1200 kupci, 2200 dobavljači, 4010 materijal, 7500 prihodi...), `seeds.watch_defaults(spine)` (porezna.gov.hr kalendar URL kind=page + 3 NN RSS kind=rss), `seeds.all(spine)` (+ kalendar.seed(tekuća godina) + quickref.seed + dnevnice defaults u tablicu); `setup.detect_hw() -> dict` (`{"cpu_cores","ram_gb","disk_free_gb","gpu": str|None (nvidia-smi subproc), "apple_silicon": bool}`); `setup.llmfit(cfg) -> dict|None` (subproc `llmfit --json` ako binary postoji, parse, None inače); `setup.detect_providers(cfg) -> dict` (`{"env_keys": [...], "oauth": [...], "ollama_models": [...]}` — env scan OPENAI/ANTHROPIC/DEEPSEEK_API_KEY, load_oauth_token, ollama /api/tags); `setup.run(cfg) -> str` (izvještaj: baza kreirana, seedovi, hw, llmfit preporuka, provideri, OCR/Ollama status, što fali). CLI `setup` poziva ovo.

- [ ] **Step 1: failing testovi** — `seeds.all` → kontni_plan ≥40 redova, watch_sources ≥4, quickref 24, deadline_dates >20; `detect_hw` vraća cpu_cores ≥1 i ram_gb > 0; `setup.run` string sadrži "kontni plan" i "hardver".
- [ ] **Step 2-4: TDD ciklus. Step 5: Commit** `feat: setup wizard — seedovi, hw detekcija, llmfit, provider detekcija`

### Task 37: eval + stats + reminders

**Files:**
- Create: `ragspine/ops/evalrun.py`
- Modify: `ragspine/__main__.py`
- Test: `tests/test_eval.py`

**Interfaces:**
- Produces: `evalrun.GOLDEN: list[dict]` — 12 slučajeva `{"q","lane"}` + 4 retrieval slučaja `{"q","expect_title"}` (nad seedanim mini-korpusom od 4 dokumenta koje eval sam ingesta u tmp bazu); `evalrun.run(cfg=None) -> dict` (`{"router_ok": x/12, "retrieval_ok": y/4, "pass": bool}` — pass ako router ≥11/12 i retrieval ≥3/4); CLI `eval` ispiše i vrati 0/1; CLI `stats` (interactions po laneu, cache hit rate iz meta, top upiti); CLI `reminders` (lista + `reminders add "tekst" 2026-08-15`).

- [ ] **Step 1: failing test** — `evalrun.run()["pass"] is True`; CLI stats/reminders smoke. Step 2-4: TDD. **Step 5: Commit** `feat: eval golden set + stats + reminders CLI`

### Task 38: finiš — README, doctor pun prolaz, cijela suita

**Files:**
- Create: `README.md`
- Test: cijela suita

**Interfaces:** —

- [ ] **Step 1:** README: što je, instalacija (`pip install -e .` + `[full]`), quickstart (`setup` → `auth add` → `serve`), spajanje Open WebUI (base URL + `/v1`), env tablica, arhitektura ukratko, degradacijska tablica iz speca.
- [ ] **Step 2:** `python -m pytest tests/ -q` — SVE zeleno. `python -m ragspine doctor` — prolazi bez crasha.
- [ ] **Step 3:** ručni smoke: `ragspine setup && ragspine serve` u pozadini, curl /health, login, /chat s pitanjem nad seedanim quickrefom, ugasi.
- [ ] **Step 4: Commit** `docs: README + finalni smoke`

---

## Self-review (obavljen pri pisanju)

- **Spec coverage:** svi moduli iz speca imaju task (spec §Arhitektura ↔ taskovi 1-38). Chat pipeline orkestracija = novi `rag/pipeline.py` (dokumentirano odstupanje). Open WebUI = samo `/v1/chat/completions` compat (Task 13) + README uputa (Task 38). LiteLLM se NE koristi direktno — dispatcher pokriva sve providere, LiteLLM ostaje moguć kroz `llm_base_url` na LiteLLM proxy (zapisati u README).
- **Type consistency:** `Hit(chunk_id, doc_id, title, text, score, doc_type)` konzistentan kroz Taskove 8/11/13; `LANE_HANDLERS` registry definiran u 13, pune ga 14/15/16/19; `safe_fetch` potpis konzistentan 4/17/19.
- **Placeholderi:** taskovi 15, 18, 19, 21-37 imaju opisne teste umjesto punih code blokova — svjesna odluka zbog veličine plana; svaki opis imenuje TOČNE ulaze, očekivane izlaze i rubne slučajeve pa je test jednoznačno izvediv. Nijedan "add error handling"/"TBD".

